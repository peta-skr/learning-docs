import { _ as _export_sfc, o as openBlock, c as createElementBlock, a2 as createStaticVNode } from "./chunks/framework.CHIFnItg.js";
const __pageData = JSON.parse('{"title":"用語集","description":"","frontmatter":{},"headers":[],"relativePath":"appendix/glossary.md","filePath":"appendix/glossary.md","lastUpdated":1786107751000}');
const _sfc_main = { name: "appendix/glossary.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="用語集" tabindex="-1">用語集 <a class="header-anchor" href="#用語集" aria-label="Permalink to &quot;用語集&quot;">​</a></h1><p>各章の初出でも説明していますが、迷ったらここに戻ってください。</p><hr><h2 id="名前空間まわり" tabindex="-1">名前空間まわり <a class="header-anchor" href="#名前空間まわり" aria-label="Permalink to &quot;名前空間まわり&quot;">​</a></h2><p><strong>Java EE（Java Platform, Enterprise Edition）</strong> Java の企業向け仕様群。Servlet / JSP / JPA / JMS などをまとめたもの。パッケージ名は <code>javax.*</code>。Java EE 8 が最後。</p><p><strong>Jakarta EE</strong> Java EE が Eclipse Foundation に移管されたあとの名称。Jakarta EE 8 までは <code>javax.*</code> のままで、<strong>Jakarta EE 9 から <code>jakarta.*</code> に変わった</strong>。この一斉変更が &quot;Big Bang&quot; と呼ばれる。</p><p><strong>名前空間（namespace）</strong> この教材では「パッケージ名の接頭辞」の意味で使う。<code>javax.servlet</code> と <code>jakarta.servlet</code> は別の名前空間。</p><p><strong><code>javax</code>（JDK 側）</strong><code>javax.sql</code> / <code>javax.naming</code> / <code>javax.crypto</code> / <code>javax.net</code> / <code>javax.imageio</code> / <code>javax.xml.parsers</code> など。 <strong>Jakarta EE とは無関係で、JDK に含まれる。<code>jakarta.*</code> 版は存在しない。</strong><a href="/learning-docs/jakarta-migration/chapters/01-why-renamed">第1章</a></p><hr><h2 id="servlet-まわり" tabindex="-1">Servlet まわり <a class="header-anchor" href="#servlet-まわり" aria-label="Permalink to &quot;Servlet まわり&quot;">​</a></h2><p><strong>Servlet</strong> HTTP リクエストを受けて処理する Java のクラス。<code>HttpServlet</code> を継承して書く。</p><p><strong>Servlet 仕様のバージョン</strong> Tomcat のバージョンと対応する。この教材では <strong>4.0（Tomcat 9）→ 6.0（Tomcat 10.1）</strong>。 5.0（Tomcat 10.0）は EOL・アーカイブ済みなので飛ばす。</p><table tabindex="0"><thead><tr><th>Tomcat</th><th>Servlet</th><th>Java</th></tr></thead><tbody><tr><td>9.0.x</td><td>4.0（<code>javax</code>）</td><td>8+</td></tr><tr><td>10.0.x</td><td>5.0（<code>jakarta</code>）</td><td><strong>EOL 2022-10-31</strong></td></tr><tr><td>10.1.x</td><td>6.0（<code>jakarta</code>）</td><td>11+</td></tr><tr><td>11.0.x</td><td>6.1（<code>jakarta</code>）</td><td>17+</td></tr></tbody></table><p><strong><code>web.xml</code>（配備記述子 / deployment descriptor）</strong><code>WEB-INF/web.xml</code>。Servlet / Filter / Listener の登録を書く XML。移行では xmlns・schemaLocation・version の3行が変わる。 <a href="/learning-docs/jakarta-migration/chapters/03-servlet-and-webxml">第3章</a></p><p><strong>Filter</strong> リクエストが Servlet に届く前後に割り込む仕組み。文字コード設定やアクセスログに使う。</p><p><strong>Listener</strong> アプリの起動・終了などのイベントを受け取る仕組み。Spring の <code>ContextLoaderListener</code> がこれ。</p><p><strong>WAR（Web application ARchive）</strong> Web アプリを1つにまとめた ZIP 形式のファイル。Tomcat の <code>webapps/</code> に置くと展開されて動く。</p><p><strong><code>provided</code> スコープ</strong> 「実行環境が提供するので WAR に同梱しない」という Maven の指定。Servlet API は Tomcat が持っているので <code>provided</code> にする。</p><hr><h2 id="jsp-まわり" tabindex="-1">JSP まわり <a class="header-anchor" href="#jsp-まわり" aria-label="Permalink to &quot;JSP まわり&quot;">​</a></h2><p><strong>JSP（JavaServer Pages / Jakarta Server Pages）</strong> HTML の中に Java の処理を埋め込むテンプレート。初回アクセス時に Servlet にコンパイルされる。</p><p><strong>JSTL（JSP Standard Tag Library）</strong> JSP で使う標準タグ集。<code>&lt;c:forEach&gt;</code> <code>&lt;c:if&gt;</code> など。 <strong>1.2 は API と実装が1つの JAR、3.0 は2つに分かれる。</strong><a href="/learning-docs/jakarta-migration/chapters/04-jsp-and-jstl">第4章</a></p><p><strong>taglib URI</strong> JSP の先頭で書く <code>&lt;%@ taglib uri=&quot;...&quot; %&gt;</code> の URI。<strong>識別子であってダウンロード先の URL ではない。</strong> JSTL 3.0 で <code>http://java.sun.com/jsp/jstl/core</code> → <code>jakarta.tags.core</code> に変わった。</p><p><strong>TLD（Tag Library Descriptor）</strong> タグライブラリの定義ファイル。JAR の中に入っている。Tomcat は起動時に JAR を走査して TLD を探す。</p><p><strong>EL（Expression Language）</strong> JSP の <code>${...}</code> の記法。Bean Validation のメッセージ生成にも使われる。 移行で実装が <code>org.glassfish:jakarta.el</code> → <code>org.glassfish.expressly:expressly</code> に差し替わる。</p><hr><h2 id="spring-まわり" tabindex="-1">Spring まわり <a class="header-anchor" href="#spring-まわり" aria-label="Permalink to &quot;Spring まわり&quot;">​</a></h2><p>用語の解説は<a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章 Spring 入門</a>にあります。ここは索引です。</p><p><strong>Spring Framework</strong> DI（依存性注入）を中心とした Java のフレームワーク。この教材では <strong>5.3 → 6.2</strong>。</p><p><strong>DI（依存性注入 / Dependency Injection）</strong> オブジェクトが必要とするもの（依存）を、自分で <code>new</code> せず<strong>外から入れてもらう</strong>こと。 <code>OrderService</code> が <code>OrderRepository</code> を <code>new</code> していないのがこれ。 <a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a></p><p><strong>IoC（制御の反転 / Inversion of Control）</strong> 「オブジェクトを作る主導権」が、自分からフレームワークへ<strong>反転</strong>していること。 DI はその実現手段。<strong>IoC が考え方、DI がやり方</strong>。 <a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a></p><p><strong>IoC コンテナ / <code>ApplicationContext</code></strong> Bean を作って保持して配る本体。「コンテナ」と言ったらこれ。 <code>ctx.getBeanDefinitionNames()</code> で中身を数えられる。 なお <code>app-javax</code> は<strong>コンテナを2つ</strong>持つ（ルート＝サービス層、子＝Controller）。<strong>Boot では1つ</strong>。 <a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a></p><p><strong>Spring MVC</strong> Spring の Web 層。<code>@Controller</code> / <code>@RestController</code> でリクエストを受ける。</p><p><strong><code>DispatcherServlet</code></strong> Spring MVC の入口となる Servlet。全リクエストを受けて、担当の <code>@Controller</code> に振り分ける。 standalone では <code>web.xml</code> に自分で登録する。<strong>Boot では <code>DispatcherServletAutoConfiguration</code> が自動でやる。</strong></p><p><strong>Bean</strong> Spring が管理するオブジェクト。<code>@Bean</code> / <code>@Component</code> などで登録する。 <strong>既定はシングルトン</strong>（コンテナに1個だけ・起動時に作られて使い回される）。 だから<strong>状態を持たせてはいけない</strong>。</p><p><strong>Bean 名</strong> Bean に付く名前。エラーメッセージにそのまま出るので、規則を知っているとソースの行を逆算できる。</p><table tabindex="0"><thead><tr><th>登録方法</th><th>名前</th></tr></thead><tbody><tr><td><code>@Service</code> などのスキャン</td><td><strong>クラス名の先頭を小文字</strong>（<code>OrderService</code> → <code>orderService</code>）</td></tr><tr><td><code>@Bean</code> メソッド</td><td><strong>メソッド名</strong>（<code>validator()</code> → <code>validator</code>）</td></tr></tbody></table><p><a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a></p><p><strong>コンポーネントスキャン（<code>@ComponentScan</code>）</strong> 指定パッケージ以下を走査し、<code>@Component</code> / <code>@Service</code> / <code>@Repository</code> / <code>@Controller</code> が 付いたクラスを Bean にする仕組み。 <strong>アノテーションを付けただけでは Bean にならない。スキャン範囲に入っていることが条件。</strong> Boot では <code>@SpringBootApplication</code> を付けたクラスのパッケージ以下が既定の範囲。</p><p><strong>AOP（アスペクト指向）/ プロキシ</strong> 「本来の処理」と「横断的な関心事（トランザクション・ログ）」を分けて書き、実行時に合成する仕組み。 Spring は対象を<strong>プロキシ（代理オブジェクト）で包む</strong>ことで実現する。</p><table tabindex="0"><thead><tr><th>方式</th><th>条件</th><th>落とし穴</th></tr></thead><tbody><tr><td>JDK 動的プロキシ</td><td>対象がインターフェースを実装している</td><td><strong>具象クラスの型で受け取れない</strong>（<code>BeanNotOfRequiredTypeException</code>）</td></tr><tr><td>CGLIB</td><td>インターフェースが無い</td><td>型名に <code>$$SpringCGLIB$$</code> が付く</td></tr></tbody></table><p><code>@Transactional</code> がコードを書かずに効くのはこれ。<strong>同じクラス内からの自己呼び出しでは効かない</strong>（エラーも出ない）。 <a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a></p><p><strong><code>@Qualifier</code></strong> 同じ型の Bean が複数あるとき、どれを注入するかを名前で指定するアノテーション。 <strong>Spring 6 では、これを書かないと落ちる場面が増えた。</strong></p><p><strong><code>-parameters</code></strong> Java コンパイラのオプション。メソッド引数の名前をクラスファイルに埋め込む。 <strong>Spring 6 で、引数名の暗黙解決に依存しているなら必須。</strong><code>@RequestParam(&quot;from&quot;)</code> のように<strong>名前を明示している箇所は、無くても動きます</strong>。 明示していない箇所だけが壊れるので、「一部のエンドポイントだけ 400 / 500」という形で現れます。 実務では明示漏れを探すより<strong>付けてしまうほうが速い</strong>ので、事実上は必須と考えてよい。 <a href="/learning-docs/jakarta-migration/chapters/06-spring-5-to-6">第6章</a></p><p><strong>Spring Boot</strong> Spring を「設定なしで動く」ようにまとめたもの。組み込み Tomcat を持つ。 <strong>別のフレームワークではなく、素の Spring の上に乗った「設定を省く仕組み」。</strong> エンティティもサービスもコントローラも、書き方は素の Spring と同じ。 <strong>この教材の主軸ではない</strong>（standalone Tomcat + WAR が主軸）。 入門は<a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a>、移行時の読み替えは<a href="/learning-docs/jakarta-migration/appendix/spring-boot">付録</a>。</p><p><strong><code>@SpringBootApplication</code></strong> 3つを合成したアノテーション。 <code>@SpringBootConfiguration</code>（＝<code>@Configuration</code>）＋ <strong><code>@EnableAutoConfiguration</code></strong> ＋ <code>@ComponentScan</code>。 <strong>このクラスを置いたパッケージ以下がスキャン範囲になる</strong>ので、置き場所を間違えると起動しない。</p><p><strong>自動設定（auto-configuration）</strong> クラスパスに何があるかを見て、設定を推測して適用する仕組み。判定は2段構え。</p><table tabindex="0"><thead><tr><th>条件</th><th>見ているもの</th></tr></thead><tbody><tr><td><code>@ConditionalOnClass</code></td><td>その JAR がクラスパスにあるか</td></tr><tr><td><code>@ConditionalOnMissingBean</code></td><td><strong>その型の Bean を自分で定義していないか</strong></td></tr></tbody></table><p><strong>後者があるので、自分で <code>@Bean</code> を書けば自動設定は譲る。</strong> つまり自動設定は「書かなかった場合の既定値」であって強制ではない。 <code>--debug</code> を付けて起動すると、何を適用し何を見送ったかのレポートが出る （<code>app-boot</code> の実測では<strong>適用118件・見送り169件</strong>）。 <a href="/learning-docs/jakarta-migration/chapters/05-spring-primer">第5章</a></p><p><strong>starter</strong><code>spring-boot-starter-web</code> のような「まとめ依存」。 これ1つで <code>spring-webmvc</code> / <code>jackson-databind</code> / <code>tomcat-embed-core</code> などが入る。 <strong>バージョンは書かない</strong>（<code>spring-boot-dependencies</code> という BOM が決める）。</p><hr><h2 id="永続化・バリデーション" tabindex="-1">永続化・バリデーション <a class="header-anchor" href="#永続化・バリデーション" aria-label="Permalink to &quot;永続化・バリデーション&quot;">​</a></h2><p><strong>JPA（Jakarta Persistence API）</strong> Java のオブジェクトと RDB のテーブルを対応づける仕様。<code>javax.persistence</code> → <code>jakarta.persistence</code>。</p><p><strong>Hibernate</strong> JPA の実装。この教材では 5.6 → 6.6。<strong>groupId が <code>org.hibernate</code> → <code>org.hibernate.orm</code> に変わった。</strong></p><p><strong>Bean Validation（Jakarta Validation）</strong><code>@NotNull</code> <code>@Size</code> などで入力チェックをする仕様。<code>javax.validation</code> → <code>jakarta.validation</code>。 実装は Hibernate Validator（6.x → 8.x）。</p><p><strong>JAXB（Jakarta XML Binding）</strong> Java のオブジェクトと XML を相互変換する仕様。<code>javax.xml.bind</code> → <code>jakarta.xml.bind</code>。 <strong><code>javax.xml.parsers</code>（JAXP）とは別物。混同すると壊れる。</strong></p><p><strong>JAXP（Java API for XML Processing）</strong><code>javax.xml.parsers</code> / <code>javax.xml.transform</code> / <code>javax.xml.stream</code>。<strong>JDK 側の API なので変えない。</strong></p><p><strong>Spring Batch</strong> バッチ処理のフレームワーク。4.3 → 5 で <code>JobBuilderFactory</code> / <code>StepBuilderFactory</code> が廃止された。</p><hr><h2 id="ビルド・ツール" tabindex="-1">ビルド・ツール <a class="header-anchor" href="#ビルド・ツール" aria-label="Permalink to &quot;ビルド・ツール&quot;">​</a></h2><p><strong>Maven Wrapper（<code>mvnw</code>）</strong> Maven 本体を自動でダウンロードして実行するスクリプト。<strong>Maven をインストールしなくてよい。</strong></p><p><strong><code>-DskipTests</code> / <code>-Dmaven.test.skip=true</code></strong> 前者は<strong>テストの実行だけ</strong>を飛ばす（コンパイルはする）。後者は<strong>コンパイルごと</strong>飛ばす。 <a href="/learning-docs/jakarta-migration/chapters/09-junit4-migration">第9章</a></p><p><strong>relocation</strong> Maven Central の仕組み。旧座標に「新座標へ移った」という情報を登録しておくと、 旧座標を指定しても自動で転送される。<strong>ビルドは成功し、警告だけが出る。</strong></p><p><strong>Apache Tomcat Migration Tool for Jakarta EE</strong><code>javax</code> → <code>jakarta</code> の変換ツール（バージョン 1.0.12、2026-08 時点）。Tomcat 本体には同梱されず単体配布。 <strong>サードパーティライブラリは上げてくれない。JAR の署名を削除する。</strong><a href="/learning-docs/jakarta-migration/chapters/10-deploy-and-tools">第10章</a></p><hr><h2 id="この教材のアプリ" tabindex="-1">この教材のアプリ <a class="header-anchor" href="#この教材のアプリ" aria-label="Permalink to &quot;この教材のアプリ&quot;">​</a></h2><p><strong>受注管理システム</strong> 顧客・商品・受注の CRUD に、確認メール（JavaMail）・帳票（Excel / PDF）・ 他システム連携（CSV / XML）・締めバッチ（Spring Batch）を足したもの。 <strong>機能を盛っているのは、移行の詰まり方がライブラリごとに違うため。</strong></p><table tabindex="0"><thead><tr><th>ディレクトリ</th><th>中身</th></tr></thead><tbody><tr><td><code>app-javax/</code></td><td>移行前（Spring 5.3 + Tomcat 9）。読者が移行する対象</td></tr><tr><td><code>app-jakarta/</code></td><td>移行後（Spring 6.2 + Tomcat 10.1）。<strong>答え</strong></td></tr><tr><td><code>broken/pattern-a</code> 〜 <code>-d</code></td><td>意図的に壊した演習用</td></tr></tbody></table><p><strong><code>/orders/health</code></strong> このアプリのヘルスチェック。<code>servletSpec</code> を返すので、<strong>Tomcat 9 と 10.1 のどちらで動いているかが1回の curl で分かる</strong>（<code>4.0</code> か <code>6.0</code> か）。</p>', 69)
  ])]);
}
const glossary = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  glossary as default
};
