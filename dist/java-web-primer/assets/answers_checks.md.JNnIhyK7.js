import { _ as _export_sfc, o as openBlock, c as createElementBlock, a0 as createStaticVNode } from "./chunks/framework.DrwlGhp1.js";
const __pageData = JSON.parse('{"title":"理解度チェックの解答","description":"","frontmatter":{},"headers":[],"relativePath":"answers/checks.md","filePath":"answers/checks.md","lastUpdated":1786937219000}');
const _sfc_main = { name: "answers/checks.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="理解度チェックの解答" tabindex="-1">理解度チェックの解答 <a class="header-anchor" href="#理解度チェックの解答" aria-label="Permalink to &quot;理解度チェックの解答&quot;">​</a></h1><p>各章末にある「理解度チェック」の解答をまとめたページです。<strong>必ず先に、自分の言葉で答えてから開いてください。</strong> 読んで納得することと、自分の言葉で言えることの間には大きな差があり、その差を測るための設問だからです。先に解答を読むと、その差が見えなくなります。</p><p>答えが下の文章と違う言い方になっていても、<strong>要点が同じなら正解</strong>です。逆に、言葉は合っているのに「なぜそうなるか」が言えないなら、それはまだ暗記です。答えられなかった章があったら、その章の該当ステップに戻ってください。各解答には根拠のある章・ステップを添えてあります。</p><h2 id="目次" tabindex="-1">目次 <a class="header-anchor" href="#目次" aria-label="Permalink to &quot;目次&quot;">​</a></h2><ul><li><a href="#序章">序章</a></li><li><a href="#第1章">第1章</a></li><li><a href="#第2章">第2章</a></li><li><a href="#第3章">第3章</a></li><li><a href="#第4章">第4章</a></li><li><a href="#第5章">第5章</a></li><li><a href="#第6章">第6章</a></li><li><a href="#第7章">第7章</a></li><li><a href="#第8章">第8章</a></li><li><a href="#第9章">第9章</a></li><li><a href="#第10章">第10章</a></li><li><a href="#第11章">第11章</a></li><li><a href="#第12章">第12章</a></li></ul><hr><h2 id="序章" tabindex="-1">序章 <a class="header-anchor" href="#序章" aria-label="Permalink to &quot;序章&quot;">​</a></h2><blockquote><p>対象: <a href="./../">序章 30分で、自分の書いたコードがブラウザに出る</a></p></blockquote><p><strong>1. <code>@WebServlet(&quot;/hello&quot;)</code> の <code>/hello</code> は、URL のどの部分に対応していたか。</strong></p><p><code>http://localhost:18081/inventory/hello</code> の<strong>一番後ろの <code>/hello</code></strong> です。コンテキストパス（<code>/inventory</code>）より後ろの、サーブレットが担当するパスにあたります。これが本当に <code>@WebServlet</code> の値で決まっていることは、ステップ6で <code>@WebServlet(&quot;/goodbye&quot;)</code> に書き換えて確かめました。<code>/hello</code> が 404 になり、<code>/goodbye</code> が応答するようになります。</p><p><strong>2. URL の <code>inventory</code> の部分（コンテキストパス）は、何によって決まっていたか。</strong></p><p><strong>WAR ファイルの名前（<code>inventory.war</code>）</strong> によって決まっていました。そしてその名前を決めていたのは <code>pom.xml</code> の <code>&lt;finalName&gt;inventory&lt;/finalName&gt;</code> です。つまりビルド設定の1行が、そのまま URL に現れています。この対応は3章のステップ6で改めて回収します。</p><p><strong>3. WAR ファイルとは何か。中に何が入っているか。</strong></p><p>WAR（Web Application Archive）は、<strong>Web アプリを1つにまとめた ZIP ファイル</strong>です。拡張子が <code>.zip</code> ではなく <code>.war</code> なだけで実体は ZIP なので、<code>unzip -l app/target/inventory.war</code> で中身を覗けます。中にはクラスファイル・<code>web.xml</code>・（後の章では）JSP や JAR が入っています。<strong>この1ファイルをサーバに置くと Web アプリが動く</strong>、というのが WAR の考え方です。序章で最初に作った WAR は 2866 バイトでした。</p><p><strong>4. <code>/inventory/items</code> を叩いたときの 404 と、<code>/inventory/</code> 自体が 404 になるときで、原因はどう違うか。</strong></p><p>見分け方は「<strong>コンテキストパスまでは合っているか</strong>」です。<code>/inventory/items</code> が 404 なら、WAR は正しくデプロイされていて動いており、<strong>その URL を担当する Servlet がいない</strong>だけです（序章の時点では <code>/items</code> を書いていないので当然）。一方 <code>/inventory/</code> 自体が 404 なら、<strong>WAR がそもそもデプロイされていない</strong>——ビルド忘れ・マウントパス間違い・起動失敗のどれかです。後者ならログに <code>deployDirectory</code> の行が出ていないはずなので、まず <code>docker compose logs tomcat</code> を見ます。</p><p><strong>5. <code>HelloServlet</code> には <code>main</code> メソッドが無い。ではこのメソッドを呼んでいるのは誰か（現時点では「分からない」が正解。3章で回収します）。</strong></p><p><strong>序章の時点では分からなくて構いません。</strong>「分からない」と答えられたなら、それが正解です。この教材はここをわざと真っ黒な箱のまま残していて、3章「誰が main を持っているのか」でその箱を開けます。先に答えだけ言うと Tomcat（<code>org.apache.catalina.startup.Bootstrap</code>）が <code>main</code> を持っていますが、大事なのは答えを覚えることではなく、<strong>「main が無いのに動くのは不思議だ」と思えている</strong>ことです（→ <a href="./../chapters/03-container">3章</a>）。</p><hr><h2 id="第1章" tabindex="-1">第1章 <a class="header-anchor" href="#第1章" aria-label="Permalink to &quot;第1章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/01-servlet">第1章 Servlet — リクエストとレスポンスの正体</a></p></blockquote><p><strong>1. <code>getContextPath()</code> と <code>getServletPath()</code> は、URL のどの部分をそれぞれ返すか。</strong></p><p><code>http://localhost:18081/inventory/hello?foo=1&amp;bar=2</code> の場合、<code>getContextPath()</code> は <code>/inventory</code>、<code>getServletPath()</code> は <code>/hello</code> を返します。この2つを合わせたものが <code>getRequestURI()</code>（<code>/inventory/hello</code>）で、クエリ文字列は含まれません。クエリは <code>getQueryString()</code> が別に持っています（<code>foo=1&amp;bar=2</code>）。<strong>序章で「<code>inventory</code> は WAR の名前、<code>hello</code> は <code>@WebServlet</code> の値」と説明したものが、Servlet 側からも別々に取り出せる</strong>ということです。</p><p><strong>2. <code>HttpServlet</code> を継承して <code>doGet</code> だけを override したとき、POST が来ると何が起きるか。なぜか。</strong></p><p><strong>405 Method Not Allowed</strong> が返ります。<code>HttpServlet</code> は HTTP メソッドごとに <code>doGet</code> / <code>doPost</code> / <code>doPut</code> / <code>doDelete</code> を持っていて、override されていないものは既定の実装が 405 を返すようになっているからです。つまり <code>doGet</code> を override するという行為は、「<strong>GET が来たときの処理を、私が引き受けます</strong>」という宣言でした。引き受けていないメソッドは断られます。</p><p><strong>3. <code>getParameter(&quot;q&quot;)</code> が返しうる3つの状態を挙げよ。</strong></p><p><code>null</code>（<code>/items</code> のようにパラメータ自体が無い）／<code>&quot;&quot;</code> 空文字列（<code>/items?q=</code> のように名前はあるが値が空）／<code>&quot;ペン&quot;</code> のような値、の3つです。<strong><code>null</code> と <code>&quot;&quot;</code> は別物</strong>なので、<code>null</code> チェックだけだと <code>NullPointerException</code> は防げても「空文字で絞り込み中」という無意味な表示が出ます。だからこの教材では常に <code>x != null &amp;&amp; !x.isBlank()</code> の形で書きます。</p><p><strong>4. <code>?q=&lt;script&gt;alert(1)&lt;/script&gt;</code> が実行されてしまう原因を、コードの1行を指して説明せよ。</strong></p><p>原因はこの行です。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">out.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;p&gt;「&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> keyword </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;」で絞り込み中&lt;/p&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>ユーザーが送ってきた <code>keyword</code> を、<strong>エスケープせずに文字列連結で HTML に直接埋め込んでいる</strong>ため、<code>&lt;script&gt;</code> がタグとしてブラウザに解釈されます。これが XSS です。しかも問題は「この1行を直せば済む」ことではありません。このコードには <code>keyword</code> <code>code</code> <code>name</code> <code>quantity</code> の4種類の埋め込みがあり、画面が増えれば埋め込み箇所は数十になります。<strong>文字列連結で HTML を作る限り、1つ忘れた瞬間に穴が開く</strong>ので構造的に避けられません。2章で JSP + <code>&lt;c:out&gt;</code> に移して、「気をつける」から「既定で安全」へ変えます。</p><p><strong>5. <code>setContentType</code> を <code>getWriter().println</code> の後に呼ぶと、なぜ効かないのか。</strong></p><p><strong>レスポンスヘッダは本文より先に送られる</strong>ためです。本文を書き始めた時点でヘッダは確定してしまうので、後から <code>setContentType</code> を呼んでも間に合いません。しかも例外は出ないので、「<code>setContentType</code> は書いてあるのに文字化けする」という追いにくいバグになります。<strong>「ヘッダの設定 → ボディの出力」の順</strong>を守ってください。</p><hr><h2 id="第2章" tabindex="-1">第2章 <a class="header-anchor" href="#第2章" aria-label="Permalink to &quot;第2章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/02-jsp">第2章 出力を HTML にすると破綻する — JSP と JSTL</a></p></blockquote><p><strong>1. <code>${item.code}</code> は、<code>Item</code> クラスのどのメソッドを呼んでいるか。その規約の名前は。</strong></p><p><code>getCode()</code> を呼んでいます。規約の名前は <strong>JavaBeans 規約</strong>です。EL は <code>code</code> というプロパティ名から <code>getCode()</code> というメソッドを探して呼びます（<code>boolean</code> なら <code>isAvailable()</code> のような <code>is～</code> も探します）。1章でアクセサを手で書いたのは、この対応を見せるためでした。<code>getCode()</code> を <code>code()</code> に改名すると <code>${item.code}</code> は動かなくなります。この規約は7章の Spring MVC でも9章の Bean Validation でも同じように効く、<strong>Java の Web 系全体を貫くルール</strong>です。</p><p><strong>2. <code>forward</code> と <code>sendRedirect</code> の違いを、ブラウザの往復回数と URL の観点で説明せよ。</strong></p><p><code>forward</code> はブラウザの往復が<strong>1回</strong>で、URL は <code>/items</code> のまま変わりません。移動はサーバ内部で起きるので、<strong>同じ <code>request</code> が渡り、<code>setAttribute</code> した値が JSP から読めます</strong>。<code>sendRedirect</code> は 302 と <code>Location</code> ヘッダを返してブラウザにもう一度叩かせるので、往復が<strong>2回</strong>になり、URL も移動先に変わります。<strong>別のリクエストになるので <code>setAttribute</code> した値は消えます</strong>。だから JSP に値を渡したい場面では <code>forward</code> です（4章の登録処理では、逆に <code>sendRedirect</code> が正解になります → <a href="./../chapters/04-filter">4章</a>）。</p><p><strong>3. JSP を <code>WEB-INF/</code> の下に置く理由は何か。</strong></p><p><strong>ブラウザから直接アクセスされないようにするため</strong>です。<code>WEB-INF/</code> 配下は Servlet の仕様で「直接アクセス不可」と決まっている安全機構で、サーバ内部からの <code>forward</code> でしか届きません。<code>webapp/item-list.jsp</code> に置くと <code>http://localhost:18081/inventory/item-list.jsp</code> で開けてしまい、Servlet を通らないので <code>items</code> が空のまま表示されたり、本来ログインが必要な画面が素通しになったりします。ステップ6で実際に直接アクセスして 404 になることを確かめました。</p><p><strong>4. <code>${keyword}</code> と <code>&lt;c:out value=&quot;${keyword}&quot;/&gt;</code> の違いは何か。どちらを使うべきか。</strong></p><p><strong>EL の <code>${...}</code> を裸で書くとエスケープされず、<code>&lt;c:out&gt;</code> で包むとエスケープされます。</strong> 判断に迷うなら、<strong>外から来た値は必ず <code>&lt;c:out&gt;</code> で包む</strong>と覚えてください。この章のコードで <code>${item.code}</code> や <code>${item.name}</code> を裸で書いているのは、商品データが自分たちで管理しているものだからで、<code>keyword</code> はユーザーが送ってきた値なので <code>&lt;c:out&gt;</code> で包んでいます。実際、<code>&lt;c:out&gt;</code> にしたことで <code>&lt;</code> が <code>&amp;lt;</code> に変換され、1章で開いていた XSS の穴が塞がりました。</p><p><strong>5. JSP の構文エラーは、いつ発見されるか。なぜビルド時ではないのか。</strong></p><p><strong>その JSP に最初のアクセスがあった瞬間</strong>に、500 エラーとして初めて現れます。<code>./mvnw package</code> でも Tomcat 起動時でも何も起きません。理由は、JSP のコンパイルを担当する Jasper が「最初にアクセスされたとき」に初めて Java へ変換して <code>javac</code> にかけるからです（2回目以降は <code>.class</code> を再利用）。つまり <strong>BUILD SUCCESS でも Tomcat が正常起動していても、JSP は壊れたままかもしれません。</strong> これが JSP の最大の弱点で、5章で「画面を実際に叩くテスト」を書く動機になります（→ <a href="./../chapters/05-junit">5章</a>）。</p><p><strong>6. JSP は最終的に何になって実行されているか。それを自分の目で確かめる方法は。</strong></p><p><strong>ただの Servlet</strong> になって実行されています。Jasper が JSP を Java のソースに変換し、<code>javac</code> でコンパイルし、普通の Servlet としてロードして実行しています。確かめる方法は、Tomcat の <code>work/</code> ディレクトリを覗くことです。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jwp-tomcat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> find</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /usr/local/tomcat/work</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -type</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> f</span></span></code></pre></div><p><code>item_002dlist_jsp.java</code> と <code>.class</code> が出てきます。中を見ると <code>extends org.apache.jasper.runtime.HttpJspBase</code>（たどれば <code>HttpServlet</code>）で、<code>_jspService(request, response)</code> の中に <code>out.write(&quot;&lt;h1&gt;商品一覧&lt;/h1&gt;\\n&quot;);</code> が並んでいます。<strong>1章であなたが手で書いていた <code>out.println</code> そのものです。</strong> つまり1章の破綻は消えたのではなく、<strong>書く人が人間から Jasper に代わっただけ</strong>でした。</p><hr><h2 id="第3章" tabindex="-1">第3章 <a class="header-anchor" href="#第3章" aria-label="Permalink to &quot;第3章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/03-container">第3章 誰が main を持っているのか — コンテナの正体</a></p></blockquote><p><strong>1. <code>main</code> メソッドを持っているのは誰か。フルクラス名で答えよ。</strong></p><p><strong><code>org.apache.catalina.startup.Bootstrap</code></strong> です。コンテナの中で <code>ps aux | grep java</code> を打つと、<code>/opt/java/openjdk/bin/java ... org.apache.catalina.startup.Bootstrap start</code> が見えます。序章から「<code>main</code> が無いのになぜ動くのか」と保留にしてきた箱の中身がこれで、<strong>唯一の <code>main</code> は Tomcat 側にあり、あなたのクラスは「登録されて、呼ばれる」側</strong>です。</p><p><strong>2. 「制御の反転」とは何か。普通の Java プログラムと何が逆なのか。</strong></p><p>普通の Java プログラムは「<strong>あなたのコード（main）がライブラリを呼ぶ</strong>」ですが、コンテナの世界では「<strong>コンテナ（main）があなたのコードを呼ぶ</strong>」に逆転します。この「呼ぶ側」と「呼ばれる側」の入れ替わりを**制御の反転（Inversion of Control, IoC）**と言います。この言葉は6章の DI で、今度は「オブジェクトの生成」の場面でまったく同じ形で再登場します。Spring の DI コンテナが「コンテナ」と呼ばれるのは、Tomcat と同じことをオブジェクトに対してやるからです（→ <a href="./../chapters/06-di">6章</a>）。</p><p><strong>3. <code>init()</code> は何回呼ばれるか。<code>doGet()</code> は何回呼ばれるか。この差から何が言えるか。</strong></p><p><code>LifecycleServlet</code> を3回叩いたときのログでは、<code>init()</code> は<strong>1回だけ</strong>、<code>doGet()</code> は<strong>3回</strong>（<code>exec-1</code> / <code>exec-2</code> / <code>exec-4</code> と毎回違うスレッドで）出ました。ここから言えるのは、<strong>Servlet のインスタンスは1個しか作られず、リクエストのたびにそれが呼ばれている</strong>ということです。加えて <code>destroy()</code> はアプリ停止時に1回だけ呼ばれます。</p><p><strong>4. Servlet のインスタンスは何個作られ、何本のスレッドがそれを使うか。</strong></p><p><strong>インスタンスは1個。それを複数のスレッドが同時に呼びます。</strong> Tomcat が1個しか作らないのは、リクエストのたびに <code>new</code> すると秒間数千リクエストでオブジェクトが数千個作られ GC の負荷になるからで、1個を使い回すほうが圧倒的に速いためです。その代償として、<strong>「Servlet に書き換わる状態を持たせてはいけない」という制約</strong>が生まれます。これは Servlet の最も重要な性質であり、最も事故を起こす性質でもあります。</p><p><strong>5. <code>count = current + 1</code> が競合するのはなぜか。CPU から見た3つの操作を挙げよ。</strong></p><p>CPU から見ると①**<code>count</code> の値を読む** ②<strong>1 を足す</strong> ③**<code>count</code> に書き戻す<strong>の3操作に分かれるからです。スレッドAが①で読んだ値をまだ③で書き戻す前に、スレッドBも①で同じ古い値を読んでしまうと、2回呼ばれたのに1回分しか増えません。これを</strong>競合状態（race condition）**と呼びます。実測では、実行前が 2 で 100 並列を投げた後に 103 になるはずが、<strong>19 でした。84回分の更新が消えています。</strong> 恐ろしいのは、<strong>1つずつ叩いたときは完璧に動いていた</strong>ことと、例外もエラーログも一切出ないことです。「ローカルでは再現しないんですよ」というバグの多くがこれです。</p><p><strong>6. <code>&lt;scope&gt;provided&lt;/scope&gt;</code> は、クラスローダの階層でいうと何を意味するか。</strong></p><p>「<strong>このクラスは親のクラスローダ（＝Tomcat）が持っているから、WAR に入れなくていい</strong>」という意味です。<code>LifecycleServlet</code> で確かめたとおり、階層は上から Bootstrap クラスローダ（表示は <code>null</code>。<code>String</code> などの JDK 本体）→ <code>URLClassLoader@378fd1ac</code>（<code>HttpServlet</code> など Tomcat が持つもの。<strong>これが <code>provided</code> の「誰か」</strong>）→ <code>ParallelWebappClassLoader (context: inventory)</code>（あなたの WAR の中身）となっています。実際 <code>unzip -l app/target/inventory.war | grep jar</code> すると JSTL の2つだけが入っていて、<code>jakarta.servlet-api</code> は入っていません。<code>provided</code> を忘れると同じ <code>HttpServlet</code> が2つ存在することになり、<code>ClassCastException: HttpServlet cannot be cast to HttpServlet</code> という読んでも意味の分からないエラーが出ます。</p><p><strong>7. <code>WEB-INF/</code> の下が直接アクセスできないのはなぜか。何を守っているか。</strong></p><p><strong>Servlet の仕様で「ブラウザから直接アクセスできない」と決められた特別なディレクトリ</strong>だからです。守っているのは、<code>WEB-INF/classes</code>（あなたの <code>.class</code>）・<code>WEB-INF/lib</code>（同梱 JAR）・<code>web.xml</code>・<code>WEB-INF/views/*.jsp</code> などです。ここが見えると<strong>アプリのソースや設定を誰でもダウンロードできてしまい</strong>、DB のパスワードが書かれた設定ファイルが公開される、といった事故になります。2章で JSP をここに置いたのも同じ理由でした。</p><p><strong>8. <code>pom.xml</code> のどの設定が、URL の <code>/inventory</code> になっていたか。</strong></p><p><code>&lt;build&gt;</code> の中の <strong><code>&lt;finalName&gt;inventory&lt;/finalName&gt;</code></strong> です。これが <code>target/inventory.war</code> と <code>target/inventory/</code> のファイル名を決め、それが <code>webapps</code> に置かれるとコンテキストパス <code>/inventory</code> になり、最終的に <code>http://localhost:18081/inventory/items</code> という URL になります。<strong>ビルド設定の1行が URL に出てくる</strong>、というのが序章から保留にしていた疑問の答えです。</p><hr><h2 id="第4章" tabindex="-1">第4章 <a class="header-anchor" href="#第4章" aria-label="Permalink to &quot;第4章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/04-filter">第4章 画面が増える — Filter と前段コントローラ</a></p></blockquote><p><strong>1. Filter と Servlet の違いを3つ挙げよ（登録方法・メソッド・役割）。</strong></p><p>登録方法は <code>@WebServlet</code> に対して <strong><code>@WebFilter</code></strong>、継承/実装は <code>extends HttpServlet</code> に対して <strong><code>implements Filter</code></strong>、メソッドは <code>doGet</code> / <code>doPost</code> に対して <strong><code>doFilter</code> の1つだけ</strong>、役割は「<strong>応答を作る</strong>」に対して「<strong>横断的な処理を挟む</strong>」です。決定的な違いは、<strong>Filter は自分で応答を返さず、<code>chain.doFilter(...)</code> で処理を次に渡すのが仕事</strong>だということ。なお<strong>インスタンスが1個</strong>である点は Servlet と同じなので、3章の制約（書き換わる状態を持たせない）がそのまま効きます。<code>EncodingFilter</code> にフィールドが1つも無いのはそのためです。</p><p><strong>2. <code>chain.doFilter(request, response)</code> を呼び忘れると何が起きるか。ログに何が出るか。</strong></p><p>そこで処理が終わり、Servlet には一生届きません。結果は <strong>200 が返るが本文が空</strong>です。そして<strong>例外もログも一切出ません</strong>。<code>if</code> の分岐の中に <code>chain.doFilter</code> を書いて <code>else</code> 側で呼び忘れる、というのが典型です。「Servlet が呼ばれない」「<code>doGet</code> の中の <code>System.out.println</code> が出ない」というときは Filter で止まっている可能性を疑ってください。</p><p><strong>3. <code>chain.doFilter</code> の前と後では、それぞれどんな処理を書けるか。例を1つずつ。</strong></p><p><code>chain.doFilter</code> は普通のメソッド呼び出しなので<strong>呼び出しから戻ってきます</strong>。だから前後の両方を掴めます。前の例は <code>AccessLogFilter</code> の <code>long start = System.currentTimeMillis();</code>（開始時刻の記録）や <code>EncodingFilter</code> の <code>setCharacterEncoding(&quot;UTF-8&quot;)</code>、後の例は <code>long elapsed = System.currentTimeMillis() - start;</code> で所要時間をログに出す処理です。他にも認証チェックは「前」でチェーンを止め、圧縮は「前」で応答をラップして「後」で書き出します。<strong>「入るときと出るときの両方を掴める」玉ねぎの皮のような構造が Filter の本質</strong>です。</p><p><strong>4. <code>@WebServlet(&quot;/app/*&quot;)</code> のとき、<code>/inventory/app/create</code> に対する <code>getContextPath()</code> / <code>getServletPath()</code> / <code>getPathInfo()</code> の値をそれぞれ答えよ。</strong></p><p><code>getContextPath()</code> は <code>/inventory</code>、<code>getServletPath()</code> は <code>/app</code>、<code>getPathInfo()</code> は <code>/create</code> です。注意すべきは <code>/inventory/app</code>（末尾なし）のとき <code>getPathInfo()</code> が <strong><code>null</code></strong> を返すことで、<code>FrontController</code> の先頭に <code>if (path == null) path = &quot;/list&quot;;</code> が書いてあるのはこのためです。これを書き忘れると <code>switch (null)</code> で <code>NullPointerException</code> になります。なお <code>getRequestURI()</code> で <code>switch</code> してはいけません。コンテキストパスが埋め込まれるので、<code>finalName</code> を変えたり ROOT にデプロイしたりすると全部壊れます。</p><p><strong>5. 登録が成功した後になぜ <code>forward</code> ではなく <code>sendRedirect</code> を使うのか。その型の名前は。</strong></p><p><code>forward</code> だと、ブラウザは「今表示しているのは <code>POST /app/create</code> の結果だ」と覚えたままになるので、<strong>ユーザーが F5 を押すと POST がもう一度飛び、商品が二重登録されます</strong>。<code>sendRedirect</code> にすると 302 でブラウザに <code>GET /app/list</code> を叩き直させるため、ブラウザが覚えているのは GET になり、F5 では一覧が再表示されるだけです。この「<strong>POST を受けたら必ずリダイレクトして GET に変える</strong>」型を <strong>PRG パターン（Post / Redirect / Get）</strong> と呼びます。</p><p><strong>6. 逆に、入力エラーでフォームに戻すときになぜ <code>forward</code> なのか。<code>sendRedirect</code> だと何が消えるか。</strong></p><p><code>setAttribute</code> で詰めた <strong><code>errors</code>（エラーメッセージ）と、ユーザーが入力した値（<code>code</code> / <code>name</code> / <code>quantity</code>）が消えます</strong>。<code>sendRedirect</code> は別のリクエストになるのでリクエストスコープが作り直されるためです。結果は「エラーになったら入力欄が全部空になった」という画面になります。<code>create</code> メソッドの中に <code>forward</code>（エラー時）と <code>sendRedirect</code>（成功時）が両方出てくるのを確認してください。</p><p><strong>7. <code>setCharacterEncoding</code> を <code>getParameter</code> の後に呼ぶとどうなるか。なぜ Filter に置くのか。</strong></p><p><strong>何も起きません（例外すら出ません）。</strong> <code>getParameter</code> を1回でも呼ぶと Tomcat はその時点の文字コードでボディ全体を解析してキャッシュするので、後から文字コードを変えても解析済みの結果は変わらないからです。だから「なるべく早い場所」に置く必要があります。<strong>Filter は「Servlet より前」という位置が構造的に保証されている</strong>のに対し、<code>doPost</code> の先頭に書くのは「気をつけて早い位置に書く」でしかなく、誰かがその上に1行足したら壊れます。なお <strong>Filter の中でも <code>chain.doFilter</code> の後ろに書いたら同じく手遅れ</strong>です。</p><p><strong>8. Tomcat 10.1 で Filter 無しでも日本語が化けなかったのはなぜか。どこを見れば分かるか。</strong></p><p>Tomcat 10.1 が全アプリ共通の設定ファイル <code>conf/web.xml</code> で、リクエストの既定文字コードを UTF-8 に決めているからです。推測せずに直接読めます。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> exec</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jwp-tomcat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sh</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;grep -n &quot;request-character-encoding&quot; /usr/local/tomcat/conf/web.xml&#39;</span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 36:  &lt;request-character-encoding&gt;UTF-8&lt;/request-character-encoding&gt;</span></span></code></pre></div><p>かつては仕様上の既定が <code>ISO-8859-1</code> だったので「EncodingFilter を書け」という記事が大量にありますが、<strong>この環境ではその前提が成立していません。</strong> ただし実験2でこの1行を <code>ISO-8859-1</code> に書き換えると、<code>%E6%97%A5%E6%9C%AC%E8%AA%9E</code>（「日本語」の UTF-8 9バイト）が <code>æ¥æ¬èª</code> に化けました。<strong>既定値は環境依存</strong>なので、「動いているから正しい」は成立しません（2章の古い <code>uri</code> に続いて2度目）。</p><p><strong>9. <code>@WebFilter</code> を2つ書いたとき、実行順序はどう決まるか。順序を指定したいならどうするか。</strong></p><p><strong><code>@WebFilter</code> では実行順序を制御できません。</strong> 順序に相当する属性が存在しない、Servlet 仕様の既知の弱点です。順序を決める手段は1つだけで、<strong><code>web.xml</code> に <code>&lt;filter-mapping&gt;</code> を書くこと</strong>。Tomcat は <code>web.xml</code> に書かれた <code>&lt;filter-mapping&gt;</code> の<strong>記述順</strong>でチェーンを組み立てます。この教材では書きません（今の2つは順序に依存しないため）が、「文字コード設定 → パラメータを読む Filter」「認証 → 権限チェック」のように順序が要る組み合わせは普通にあります。7章以降の Spring では <code>FilterRegistrationBean</code> の <code>order</code> や <code>@Order</code> で書けるようになります。</p><p><strong>10. <code>FrontController</code> は7章で何に置き換わるか。それでも自分で書く価値は何か。</strong></p><p>Spring MVC の <strong><code>DispatcherServlet</code></strong> に置き換わります（→ <a href="./../chapters/07-spring-mvc">7章</a>）。<code>Dispatcher</code>（振り分け役）+ <code>Servlet</code>、つまり今あなたが書いている <code>switch</code> と同じものです。**このクラスは「捨てるために書く」**もので、価値は、7章で <code>DispatcherServlet</code> の名前を見たときに「ああ、あの <code>switch</code> か」と分かることにあります。自分で一度書いた仕組みは、フレームワークが持っていても中身が見えます。</p><hr><h2 id="第5章" tabindex="-1">第5章 <a class="header-anchor" href="#第5章" aria-label="Permalink to &quot;第5章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/05-junit">第5章 手で確認するのをやめる — JUnit 5</a></p></blockquote><p><strong>1. <code>&lt;scope&gt;test&lt;/scope&gt;</code> と <code>&lt;scope&gt;provided&lt;/scope&gt;</code> の違いを、WAR の中身とコンパイル可否の観点で説明せよ。</strong></p><p>どちらも <strong>WAR には入りません</strong>が、コンパイル可否が違います。<code>provided</code> は「Tomcat が持っているから WAR に入れない」もので、<strong><code>src/main/java/</code> からも使えます</strong>。<code>test</code> は「テストのときだけ使うから WAR に入れない」もので、<strong><code>src/main/java/</code> からは使えず、<code>import</code> するとコンパイルエラーになります</strong>。これは事故ではなく設計で、テスト用のコードが本番の WAR に紛れ込むのを防いでいます。既定の <code>compile</code>（JSTL がこれ）は、コンパイル時もテスト時も使えて WAR にも入ります。</p><p><strong>2. <code>assertEquals</code> の第1引数と第2引数はどちらが期待値か。逆にすると何が起きるか。</strong></p><p>**第1引数が期待値（expected）、第2引数が実際の値（actual）**です。逆にしてもテストは通ってしまうので気づけません。気づくのは失敗したときで、<strong>失敗メッセージが嘘をつきます</strong>。<code>assertEquals(5, actual)</code> が失敗すれば <code>expected: &lt;5&gt; but was: &lt;6&gt;</code>（正しい日本語）ですが、逆に書くと <code>expected: &lt;6&gt; but was: &lt;5&gt;</code> となり、6 が期待値であるかのように読めてしまい、直すべき場所を間違えます。<strong>リテラルが左</strong>と覚えると外しません。</p><p><strong>3. 単体テストと結合テストで、起動する必要があるものはそれぞれ何か。この章での実測時間は何秒対何秒だったか。</strong></p><p>単体テスト（<code>ItemStoreTest</code>）が起動するのは <strong>JVM だけ</strong>で、Docker も Tomcat も WAR も要りません。結合テスト（<code>ItemHttpIT</code>）は <strong>Docker + Tomcat + WAR のデプロイ</strong>が要り、事前に <code>./mvnw package</code> と <code>docker compose restart tomcat</code> が済んでいる必要があります。実測は <strong>単体 5件で 0.014 秒、結合 6件で 0.426 秒</strong>で、クラス全体で約 <strong>30倍</strong>（1件あたりでは 2.8ms 対 71ms で約 25倍）の差でした。今はどちらも一瞬ですが、テストが 500 本になれば「7秒」と「3分半」の差になります。だから<strong>速いテストで大半を確認し、遅いテストは経路が通っているかだけに絞る</strong>のが基本方針です。</p><p><strong>4. <code>HttpClient</code> の <code>followRedirects</code> を <code>NEVER</code> にした理由は何か。既定のままだとどのテストが壊れるか。</strong></p><p><strong>302 を観測できるようにするため</strong>です。既定の <code>Redirect.NORMAL</code> だと <code>HttpClient</code> が勝手にリダイレクト先へ飛んでしまい、テストは 200 を観測して 302 を検証できません。壊れるのは <strong><code>create_redirects()</code></strong>（「登録に成功するとリダイレクトする」）で、302 と <code>Location</code> ヘッダが <code>/app/list</code> で終わることを検証しているテストです。<code>curl</code> に <code>-L</code> を付けるかどうかと同じ話です。</p><p><strong>5. <code>assertTrue</code> と <code>assumeTrue</code> の違いは何か。<code>assumeTrue</code> を使う代償は何か。</strong></p><p>条件が偽のとき、<code>assertTrue</code> は<strong>テストを失敗（赤）<strong>にし、<code>assumeTrue</code> は</strong>スキップ</strong>にします。使い分けの意味は、「<strong>アプリが壊れている</strong>」と「<strong>前提条件が揃っていない</strong>」を区別することです。Tomcat を起動し忘れただけで赤くなると「テストが赤い＝アプリのバグ」という信号が汚染され、赤を見ても慌てなくなり、テストの価値がそこで死にます。代償は、<strong>テストが黙って消えても緑になる</strong>ことです。実測では Tomcat を止めると <code>ItemHttpIT</code> の行が <code>Tests run: 0</code> になり、<code>Skipped: 0</code>——「スキップした」とすら出ないまま <code>BUILD SUCCESS</code> でした（<code>@BeforeAll</code> で <code>assumeTrue</code> を使うとクラス全体が実行前に打ち切られるため）。</p><p><strong>6. <code>ItemHttpIT</code> が実行されていないのに <code>BUILD SUCCESS</code> になったのはなぜか。どう直したか。</strong></p><p>テストを実行している surefire プラグインの<strong>既定のファイル名パターン</strong>（<code>**/Test*.java</code> / <code>**/*Test.java</code> / <code>**/*Tests.java</code> / <code>**/*TestCase.java</code>）に、<strong><code>ItemHttpIT</code> はどれにも一致しなかった</strong>からです。実行されていないのでもちろん失敗もせず、緑になります。<code>./mvnw test -Dtest=ItemHttpIT</code> と名前を明示すれば 6本とも動いて全部通ったので、<strong>テストのコードは正しく、呼ばれていなかっただけ</strong>でした。直し方は <code>pom.xml</code> に <code>maven-surefire-plugin</code> 3.5.2 を明示し、<code>&lt;includes&gt;</code> に <code>**/*Test.java</code> と <code>**/*IT.java</code> を書くことです。ただし **<code>&lt;includes&gt;</code> は「追加」ではなく「置き換え」**なので、既定パターンは全部消えます。書き忘れると、また実行されないテストが生まれます。</p><p><strong>7. <code>Tests run:</code> の合計を毎回見なければならない理由を、この章に出た2つのケースを挙げて説明せよ。</strong></p><p><strong>原因が違うのに出力がまったく同じ</strong>で、どちらも <code>BUILD SUCCESS</code> になるからです。1つ目はステップ5の「名前が surefire のパターンに合わなくて実行されなかった」（11 → 5）、2つ目はステップ6の「Tomcat が止まっていて <code>assumeTrue</code> でクラスごと打ち切られた」（11 → 5）。どちらも緑ですが、何も守られていません。<strong><code>Tests run:</code> の数字だけが真実を教えてくれます。</strong> この状態は「テストが無いより悪い」——テストが無ければ慎重になりますが、緑があると安心してしまうからです。だから以降の全章の完了条件は「緑」だけでなく「<strong>件数が期待どおりか</strong>」まで含みます。</p><p><strong>8. <code>new ItemStore()</code> を2回しても中身が共有されるのはなぜか。それがテストの何を壊すか。</strong></p><p><code>ItemStore</code> のリストが <strong><code>private static final List&lt;Item&gt; ITEMS</code></strong> と <code>static</code> で宣言されているからです。<code>static</code> はクラスに1つであってインスタンスごとではないので、何個 <code>new</code> しても中身のリストは1個しかありません。壊れるのはテストの鉄則、「<strong>どの1本を、どの順番で実行しても、同じ結果になること</strong>」です。実験（<code>SharedStateProbeTest</code>）では、テストAが1件足した後にテストBが <code>new ItemStore()</code> して 5件を期待したところ <strong><code>expected: &lt;5&gt; but was: &lt;6&gt;</code></strong> で落ちました。単独実行なら通るのに、Aの後だと落ちる——「私の環境では通るのに CI では落ちる」「順番を変えたら落ちた」というバグの正体です。</p><p><strong>9. <code>FrontController</code> に <code>FakeItemStore</code> を渡せないのはなぜか。コードのどの1行が原因か。</strong></p><p>原因はこの1行です。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">private</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> final</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ItemStore store </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ItemStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p><code>store</code> を<strong>その場で <code>new</code> している</strong>ため、コンストラクタから渡す口も、セッターもありません。<code>private</code> なので外から代入できず、しかも <code>final</code> なので仮に見えても代入できません。<code>FakeItemStore extends ItemStore</code> を書くことはできますが、<strong>刺す口が無いのでどこにも刺さりません</strong>。（Mockito のようなモックライブラリでリフレクションで押し込むことはできますが、<strong>それは問題を隠しているだけ</strong>です。「テストのために特殊なライブラリで無理やりこじ開けないといけない設計」であることが問題そのものなので、この教材ではモックライブラリを使いません。）</p><p><strong>10. 「アプリを動かすには共有したい / テストするには分離したい」という板挟みを、<code>new</code> のままで解ける方法はあるか。無いとすれば、なぜか。</strong></p><p><strong>解けません。</strong> <code>new</code> するのは<strong>使う側</strong>（<code>FrontController</code> や、6章で作る <code>ItemService</code>）なので、使う側の数だけインスタンスができます。共有したければ <code>static</code> に逃げるしかなく、<code>static</code> に逃げるとテストが独立できません。逆に <code>static</code> を外すと、<code>ItemService</code> の中にも <code>new ItemStore()</code> と書いた瞬間に <code>FrontController</code> の <code>store</code> と別物になり、登録した商品が一覧に出なくなります。</p><p>根っこは1つです。</p><blockquote><p><strong>オブジェクトを、それを使うクラス自身が <code>new</code> している。だから「誰が」「いくつ」「どれを」作るかを、外から決められない。</strong></p></blockquote><p>だから、テストのときだけ別のものに差し替えることが原理的にできません。これを解くのが、生成の主導権を外に渡す <strong>DI（依存性注入）</strong> です。3章の「制御の反転」を、今度はオブジェクトの生成の場面でやります（→ <a href="./../chapters/06-di">6章</a>）。</p><hr><h2 id="第6章" tabindex="-1">第6章 <a class="header-anchor" href="#第6章" aria-label="Permalink to &quot;第6章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/06-di">第6章 <code>new</code> が壊れる — 依存性注入とミニ DI コンテナ</a></p></blockquote><p><strong>1. 「<code>new</code> された依存は差し替えられない」とはどういうことか。5章のどのテストがそれで書けなかったか。</strong></p><p>使う側のクラスが <code>private final ItemStore store = new ItemStore();</code> と自分で <code>new</code> してしまうと、<strong>「ItemStore という具体的なクラスを使う」がコードに焼き付き、外から口を出す隙間が1ミリも残りません</strong>。<code>private</code> で <code>final</code> なので、コンストラクタからもセッターからも代入できません。5章で書けなかったのは、<strong><code>FrontController.create()</code> の「そのコードはすでに使われています」という重複チェックの単体テスト</strong>です。<code>FakeItemStore</code> を書いても刺す口が無く、結局 <code>ItemHttpIT</code> で HTTP 越しに 71ms かけて確認するしかありませんでした（→ <a href="./../chapters/05-junit">5章</a>）。</p><p><strong>2. DI（依存性注入）と DI コンテナの違いを説明せよ。コンテナ無しで DI はできるか。</strong></p><p>**DI は「設計」、DI コンテナは「道具」**です。DI とは「<strong>自分が使うものを、自分で作らず、外から渡してもらう</strong>」という、それだけの設計。DI コンテナは、その「外から渡す」組み立て作業を自動化する部品です。<strong>コンテナ無しで DI はできます。</strong> 実際この章のステップ4では、コンテナを1行も書かずに</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ItemService service </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ItemService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(repository);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 手で渡す ← これが DI</span></span></code></pre></div><p>と手で渡しただけで、5章の困りごとは<strong>もう解決していました</strong>。ここを混ぜると「Spring を入れないと DI できない」という誤解になります。コンテナが要るのは、この後の<strong>別の破綻</strong>（本番側の組み立てがクラスの増加で爆発する）に対してです。</p><p><strong>3. <code>ItemService</code> がコンストラクタで <code>ItemRepository</code> を受け取ると、なぜテストが書けるようになるのか。</strong></p><p><code>ItemService</code> が「誰が渡してくるか」を知らなくなり、<strong><code>ItemRepository</code> を満たすものなら何でも受け付けられる</strong>ようになるからです。だからテストからは <code>new ItemService(new FakeItemRepository())</code> と、手で偽物を渡せます。その結果、5章の3つの困りごとが全部解けました。(1) <code>FakeItemRepository</code> はテストごとに <code>new</code> するのでリストも別々になり、<strong><code>static</code> による干渉が起きない</strong>。(2) 偽物は<strong>空から始まる</strong>ので、「最初から5件」という前提に縛られず、必要な状態を自分で作れる。(3) 業務ロジックの検証に <strong>Tomcat も HTTP も要らない</strong>。とくに「空入力でエラーがちょうど2つ」のような検証は、HTTP 越しでは HTML をパースする必要があってほぼ不可能でしたが、いまは <code>errors.size()</code> を見るだけです。</p><p><strong>4. コンストラクタ注入を setter 注入より優先する理由を3つ挙げよ。</strong></p><p>(1) <strong><code>final</code> にできる</strong>——一度渡したら二度と差し替わらない。3章の「Servlet に書き換わる状態を持たない」と同じ思想です。(2) <strong>渡し忘れがコンパイルエラーになる</strong>——引数が足りないのでビルドが赤くなります。setter 注入だと渡し忘れたまま起動して <code>NullPointerException</code> で初めて気づきます。(3) <strong>依存が引数の一覧として見える</strong>——引数が5個あればこのクラスは5個に依存しており、<strong>引数が増えすぎたら仕事を持ちすぎているという設計上の警報</strong>になります。フィールド注入だと依存がいくつあっても引数は0個なので、この警報が鳴らなくなります。「書きやすい」と「壊れにくい」が逆向きになる典型例です。</p><p><strong>5. <code>@Retention(RetentionPolicy.RUNTIME)</code> を書き忘れると何が起きるか。既定値は何か。症状は。</strong></p><p>既定値は <strong><code>RetentionPolicy.CLASS</code></strong> です。これは「<code>.class</code> ファイルには書いてあるが、JVM がメモリに読み込むときに捨てる」という状態なので、<strong><code>getAnnotation</code> が常に <code>null</code> を返します</strong>。症状は、コンパイルは通り、<code>.class</code> を逆アセンブルすれば <code>@Component</code> は入っている（＝ちゃんと付いているように見える）のに、<code>MiniContainer.register()</code> が<strong>すべてのクラスを拒否</strong>して</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>java.lang.IllegalArgumentException: @Component が付いていない: com.example.inventory.InMemoryItemRepository</span></span></code></pre></div><p>が出ることです。「付いてるじゃないか」と何時間も悩むタイプのエラーなので、<strong><code>getAnnotation</code> が意味不明に <code>null</code> を返したら、まず <code>@Retention(RetentionPolicy.RUNTIME)</code> を疑ってください。</strong> なお <code>SOURCE</code> は <code>.class</code> にも残りません。</p><p><strong>6. リフレクションを一言で定義せよ。この章で使った4つの API とその戻り値を答えよ。</strong></p><blockquote><p><strong>リフレクションとは、クラスの構造を実行時にプログラムから読む機能です。</strong></p></blockquote><p>普通の Java はコンパイル時に型が決まりますが、リフレクションは実行時に <code>Class</code> オブジェクトに尋ねてから決めます。「<strong>クラスを、データとして扱う</strong>」と考えてください。使った API は4つだけです。</p><table tabindex="0"><thead><tr><th>API</th><th>戻り値</th></tr></thead><tbody><tr><td><code>type.getAnnotation(Component.class)</code></td><td>付いていればアノテーションのオブジェクト、付いていなければ（または <code>RUNTIME</code> でなければ）<code>null</code></td></tr><tr><td><code>impl.getDeclaredConstructors()</code></td><td>そのクラスのコンストラクタの配列</td></tr><tr><td><code>constructor.getParameterTypes()</code></td><td>そのコンストラクタの引数の型の配列</td></tr><tr><td><code>constructor.newInstance(args)</code></td><td>実際に作られたインスタンス</td></tr></tbody></table><p><strong>この4つで DI コンテナが書けます。</strong></p><p><strong>7. <code>MiniContainer.get(ItemService.class)</code> を呼んだとき、<code>InMemoryItemRepository</code> と <code>ItemService</code> のどちらが先に作られるか。なぜか。</strong></p><p><strong><code>InMemoryItemRepository</code> が先です。</strong> <code>MiniContainer</code> の核心は</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">arguments[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(parameterTypes[i]);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★再帰</span></span></code></pre></div><p>という<strong>再帰</strong>にあります。<code>ItemService</code> を作ろうとすると、そのコンストラクタの引数の型（<code>ItemRepository</code>）を見て、それを作るために <code>get</code> を再帰的に呼びます。引数がすべて揃って初めて <code>newInstance</code> が呼べるので、<strong>依存されている側から順に、底から作られる</strong>わけです。だから <code>ItemService</code> の生成は、<code>InMemoryItemRepository</code> が完成するまで待たされます。</p><p><strong>8. <code>bindings</code> に <code>ItemRepository → InMemoryItemRepository</code> を入れているのはなぜか。この1行が無いと何が起きるか。</strong></p><p><code>register</code> の中の、実装しているインターフェースからも引けるようにするこの部分です。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (Class&lt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt; face </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> type.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getInterfaces</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) {</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    bindings.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(face, type);</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>ItemService</code> が要求するのは <strong>インターフェースの <code>ItemRepository</code></strong> であって、具象クラスの名前ではありません。この対応が無いと <code>bindings.get(ItemRepository.class)</code> が <code>null</code> になり、</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>IllegalStateException: 登録されていない型を要求された: com.example.inventory.ItemRepository</span></span></code></pre></div><p>で止まります。<strong>この「インターフェースからも引ける」1行が、DI コンテナの本質的な機能</strong>です。</p><p><strong>9. <code>singletons.put</code> を <code>impl</code> と <code>type</code> の2回呼んでいる理由を説明せよ。</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">singletons.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(impl, instance);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// InMemoryItemRepository.class → その1個</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">singletons.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">put</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(type, instance);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ItemRepository.class         → 同じ1個</span></span></code></pre></div><p><strong>インターフェースの名前でも具象クラスの名前でも、同じ1個が返るようにするため</strong>です。片方だけだと、後で <code>get(InMemoryItemRepository.class)</code> と書いたときに<strong>もう1個作られてしまいます</strong>。<code>MiniContainerTest</code> の <code>returns_same_instance</code> が、<code>assertSame(container.get(ItemRepository.class), container.get(InMemoryItemRepository.class))</code> でまさにこれを検証しています。</p><p><strong>10. <code>resolving</code> リストは何のためにあるか。無いとどうなるか。</strong></p><p><strong>循環依存を検出するため</strong>です。<code>resolving</code> は「いま作っている最中の型」のリストで、いま作ろうとしている型がすでにその中にあれば、A が B を要求し B が A を要求している状態だと分かります。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (resolving.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">contains</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(impl)) {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> IllegalStateException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;循環依存を検出した: &quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resolving </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot; -&gt; &quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> +</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> impl.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">());</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>これが無いと<strong>再帰が無限に続き、<code>StackOverflowError</code> で落ちます</strong>。エラーメッセージに <code>resolving</code> の中身をそのまま入れているので、どの型がどの順で絡んでいるかが読めます。なお <code>resolving.remove</code> を <strong><code>try-finally</code></strong> で行うのが重要で、<code>finally</code> にしないと依存の解決に失敗して例外が飛んだとき型が残りっぱなしになり、次に同じ型を要求したときに「循環依存を検出した」という<strong>まったく的外れなエラー</strong>が出ます。（4章の <code>AccessLogFilter</code> は意図的に <code>try-finally</code> を使わなかった例、こちらは手を抜いてはいけない例です。）</p><p><strong>11. <code>ContainerHolder</code> が「妥協」なのはなぜか。DI とサービスロケータの違いは。7章でどう解消されるか。</strong></p><p><code>FrontController</code> が <code>ContainerHolder.container().get(ItemService.class)</code> と<strong>静的メソッドを直接呼んで、依存を自分で取りに行っている</strong>からです。これは DI ではなく<strong>サービスロケータ</strong>という別のパターンです。</p><table tabindex="0"><thead><tr><th></th><th>DI</th><th>サービスロケータ</th></tr></thead><tbody><tr><td>依存の入手</td><td><strong>渡してもらう</strong></td><td><strong>自分で取りに行く</strong></td></tr><tr><td>依存の可視性</td><td>コンストラクタの引数に<strong>全部見える</strong></td><td>メソッドの中に<strong>散らばる</strong></td></tr><tr><td>テスト</td><td>偽物を<strong>渡せる</strong></td><td><code>static</code> なので<strong>差し替えにくい</strong></td></tr></tbody></table><p>しかも <code>ContainerHolder.container</code> は <code>static</code> フィールドで、この章の冒頭で「<code>static</code> が悪さをする」と散々書いたばかりのものを1つ導入しています。<strong>なぜ許したのか</strong>——Servlet を <code>new</code> するのは Tomcat であり、その生成に自分たちが1ミリも関与できないからです。<strong>Tomcat が作るものには、コンストラクタ注入ができない。</strong> だから「どこか1箇所から取りに行く」入口が要る。<code>ContainerHolder</code> はその「1箇所」を1箇所に閉じ込め、<code>FrontController.init()</code> にしか出てこないよう染み出しを最小にした妥協です。<strong>7章で消えます。</strong> Spring MVC の <code>DispatcherServlet</code> を使うと Spring 側が Controller の生成にも関与するので、Controller にコンストラクタ注入ができるようになります（→ <a href="./../chapters/07-spring-mvc">7章</a>）。</p><p><strong>12. <code>ItemServiceTest</code>（0.013秒）と <code>ItemHttpIT</code>（0.565秒）の差は何から来ているか。それでも <code>ItemHttpIT</code> を消してはいけない理由を2つ挙げよ。</strong></p><p>差は約 <strong>43倍</strong>で、来ているのは <strong>HTTP の往復と、Tomcat / Filter / DispatcherServlet / JSP のレンダリングという配線全体を通していること</strong>です。<code>ItemServiceTest</code> はただの Java のクラスを呼ぶだけなので、<strong>環境を一切要求しません</strong>（Tomcat 未起動で黙ってスキップされる危険もない）。それでも <code>ItemHttpIT</code> を消してはいけない理由は、<strong>(1) 単体テストは「判断」しか検証せず、「配線」を検証しないから</strong>——URL のマッピング、Filter が効いているか、JSP が本当にレンダリングされるか、302 の <code>Location</code> が正しいかは HTTP 越しでしか分かりません。<strong>(2) 単体テストが全部緑でもアプリが起動時に落ちることがあるから</strong>——たとえば <code>ContainerHolder</code> の登録漏れは単体テストでは絶対に見つかりません。<strong>単体テストで「判断」を、結合テストで「配線」を検証する</strong>、という分担です。</p><hr><h2 id="第7章" tabindex="-1">第7章 <a class="header-anchor" href="#第7章" aria-label="Permalink to &quot;第7章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/07-spring-mvc">第7章 自前ディスパッチャを捨てる — Spring MVC</a></p></blockquote><p><strong>1. 6章で自作した <code>MiniContainer</code> / <code>ContainerHolder</code> / <code>FrontController</code> は、それぞれ Spring の何に置き換わったか。</strong></p><p><code>MiniContainer</code> は <strong><code>ApplicationContext</code></strong>、<code>ContainerHolder</code> の <code>register</code> は <strong><code>@ComponentScan</code></strong>、<code>FrontController</code> の <code>switch</code> は <strong><code>DispatcherServlet</code> + <code>@GetMapping</code> / <code>@PostMapping</code></strong> に置き換わりました。ほかにも <code>request.getParameter(...)</code> → メソッド引数、<code>Integer.parseInt</code> + try-catch → 引数の型による自動変換、<code>request.setAttribute(...)</code> → <code>Model</code>、<code>forward(&quot;/WEB-INF/views/&quot; + v + &quot;.jsp&quot;)</code> → ビュー名を <code>return</code> + <code>ViewResolver</code>、<code>sendRedirect(ctx + &quot;/app/list&quot;)</code> → <code>return &quot;redirect:/app/list&quot;</code> と対応します。自作 <code>@Component</code> / <code>@Inject</code> は Spring の <code>@Service</code> / <code>@Repository</code>（コンストラクタが1つなら注釈不要）です。</p><p><code>ApplicationContext</code> の実装は数万行ありますが、骨格は6章の110行と同じです。パッケージを走査してクラスを集め、アノテーションが付いているか判定し、コンストラクタの引数の型を調べ、その型の Bean を再帰的に解決し、インターフェースを実装クラスに解決し、1個だけ作って使い回す——<strong>あなたが全部書いたことです。</strong> Spring が数万行あるのは、スコープ・循環参照の検出・遅延初期化・プロキシ・条件付き登録など周辺のケースを全部面倒見ているからであって、<strong>中心の考え方は増えていません</strong>。コードは消えますが、理解は残ります。</p><p><strong>2. <code>spring-webmvc</code> に <code>&lt;scope&gt;provided&lt;/scope&gt;</code> を付けてはいけない理由を、3章のクラスローダの図を使って説明せよ。</strong></p><p>3章で見たとおり <code>provided</code> は「<strong>親のクラスローダ（＝Tomcat）が持っているから WAR に入れなくていい</strong>」という意味でした。しかし <strong>Tomcat は Spring を1バイトも持っていません。</strong> 親の <code>URLClassLoader</code> にあるのは <code>HttpServlet</code> や <code>jakarta.servlet-api</code>、Jasper といった<strong>仕様 API だけ</strong>で、Spring はそこに存在しません。だから Spring は WAR に同梱して、子の <code>ParallelWebappClassLoader</code> に読ませる必要があります。実際 <code>unzip -l</code> すると <code>WEB-INF/lib/</code> に <code>spring-webmvc-6.2.19.jar</code> 以下8個の JAR が入っています。</p><p>厄介なのは、<strong>間違えて <code>provided</code> を付けてもビルドは通る</strong>ことです。起動時に <code>ClassNotFoundException: org.springframework.web.servlet.DispatcherServlet</code> で落ちます。ビルドが緑なので原因に辿り着くまで時間がかかる類のエラーです。<strong>判定基準は1つ、「Tomcat がそのライブラリを持っているか」だけ</strong>です。</p><p><strong>3. <code>@ComponentScan</code> は、6章のどのコードの自動化か。走査で何を見て登録を判断しているか。</strong></p><p>6章の <code>ContainerHolder</code> に手で書いていたこの2行の自動化です。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">c.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(InMemoryItemRepository.class);   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 手で1行</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">c.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">register</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ItemService.class);              </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 手で1行</span></span></code></pre></div><p><code>@ComponentScan(basePackages = &quot;com.example.inventory&quot;)</code> は「そのパッケージ以下のクラスを全部見て、<strong>目印が付いているものをコンテナに登録せよ</strong>」という意味です。判断しているのは <strong><code>@Component</code> 系のアノテーション（<code>@Repository</code> / <code>@Service</code> / <code>@Controller</code> / <code>@Configuration</code>）が付いているか</strong>で、<code>Item</code> や <code>ItemRepository</code>（インターフェース）や <code>EncodingFilter</code> のように目印が無いものは登録しません。<strong>やっていることは6章の <code>register</code> とまったく同じ</strong>で、「手で列挙するか、走査させるか」だけが違います。走査に使っているのも6章の道具箱に置いたリフレクションの延長です（Spring はさらに賢く、クラスをロードする前にバイトコードを直接読んで判定していますが、考え方は同じです）。</p><p><strong>4. <code>@EnableWebMvc</code> が登録する部品を3つ挙げ、それぞれ <code>FrontController</code> の何に相当するか答えよ。</strong></p><table tabindex="0"><thead><tr><th>登録されるもの</th><th>6章の何に相当するか</th></tr></thead><tbody><tr><td><code>RequestMappingHandlerMapping</code></td><td><code>switch (path)</code> の対応表</td></tr><tr><td><code>RequestMappingHandlerAdapter</code></td><td><code>case &quot;/list&quot; -&gt; list(req, res)</code>（見つけたメソッドを呼ぶ）</td></tr><tr><td>引数リゾルバ群</td><td><code>request.getParameter(...)</code></td></tr><tr><td>型変換サービス</td><td><code>Integer.parseInt(...)</code></td></tr><tr><td>戻り値ハンドラ群</td><td><code>forward(...)</code></td></tr></tbody></table><p>つまり <strong><code>@EnableWebMvc</code> は、<code>FrontController.handle</code> がやっていた仕事を部品に分解して全部登録する1行</strong>です。この章で一番「魔法に見える」1行でもあります。書き忘れると <code>DispatcherServlet</code> は起動するのに <code>@GetMapping</code> が1つも認識されず、<strong>全部 404</strong> になります。</p><p><strong>5. <code>@Bean</code> と <code>@Service</code> は、どう使い分けるか。判断基準を1文で述べよ。</strong></p><blockquote><p><strong>自分が書いたクラスなら <code>@Component</code> / <code>@Service</code> / <code>@Repository</code> + <code>@ComponentScan</code>、他人（ライブラリ）のクラスや生成に手順が要るものは <code>@Bean</code> メソッド。</strong></p></blockquote><p><code>@ComponentScan</code> は「クラスに目印を付けて自動登録」ですが、<strong>自分で書いていないクラスには目印を付けられません</strong>。<code>InternalResourceViewResolver</code> は Spring のクラスなので、<code>WebConfig</code> の <code>@Bean</code> メソッドで手動登録しています。まったく同じ理屈が <code>web.xml</code> にも当てはまります——<code>DispatcherServlet</code> は Spring のクラスなので <code>@WebServlet</code> を書き足せず、外側から <code>web.xml</code> で登録するしかありません。</p><p><strong>6. <code>@GetMapping(&quot;/list&quot;)</code> と <code>return &quot;redirect:/app/list&quot;</code> で <code>/app</code> の有無が違うのはなぜか。それぞれのパスの「基準点」を答えよ。</strong></p><p><strong>基準点が違う</strong>からです。</p><ul><li><strong><code>@GetMapping</code> / <code>@PostMapping</code> に書くパス</strong>——基準点は**<code>DispatcherServlet</code> が受け取った時点**。<code>web.xml</code> に <code>&lt;url-pattern&gt;/app/*&lt;/url-pattern&gt;</code> と書いた時点で <code>/app</code> は「サーブレットパス」になっており、<code>DispatcherServlet</code> が見るときにはコンテキストパスもサーブレットパスも切り取られています。だから <code>/app</code> は<strong>書きません</strong>。</li><li><strong><code>&quot;redirect:&quot;</code> に書くパス</strong>——基準点は<strong>コンテキストパスの直後</strong>。ブラウザに「次はここへ行け」と返すので、サーブレットパスも含めた完全な道順が要ります。だから <code>/app</code> を<strong>書きます</strong>（<code>/inventory</code> は Spring が足してくれます）。</li></ul><p>判定は1問で済みます。</p><blockquote><p><strong>そのパスは「誰に渡すもの」か。</strong> <code>DispatcherServlet</code> に「どのメソッドか」を教えるなら <code>/app</code> を書かない。ブラウザに「次にここを叩け」と教えるなら <code>/app</code> を書く。</p></blockquote><p>この章では実際に <code>@GetMapping(&quot;/app/list&quot;)</code> と書いて 404 を踏みました。<strong>ビルドは成功し、Tomcat も正常起動し、エラーは1つも出ませんでした。</strong> ログに <code>No mapping for GET /inventory/app/list</code> が出ていて、そこで初めて分かります。これは4章で <code>getRequestURI()</code> ではなく <code>getPathInfo()</code> を使った理由とまったく同じ話で、<strong>あなたは4章ですでに正解を書いていました</strong>（→ <a href="./../chapters/04-filter">4章</a>）。なお JSP の <code>action</code> は<strong>ブラウザに渡すパス</strong>なので <code>/app</code> が要り、JSP は7章で1文字も変えていません。</p><p><strong>7. <code>@RequestParam(&quot;delta&quot;) int delta</code> と書くだけで 400 が返るのはなぜか。6章では同じことを何行で書いていたか。</strong></p><p>HTTP は文字列しか運べないので、<code>delta=abc</code> が来たとき <strong><code>@EnableWebMvc</code> が登録した型変換サービス</strong>が「引数の型は <code>int</code> だ → <code>&quot;abc&quot;</code> を <code>int</code> にできない」と判定し、<code>MethodArgumentTypeMismatchException</code> を経て <strong>400 Bad Request</strong> を返すからです。実測でも <code>curl -X POST --data &quot;code=S-100&amp;delta=abc&quot;</code> は <code>400</code> を返しました。6章では<strong>7行</strong>（宣言 + try-catch）を手で書いていました。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> delta;</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    delta </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Integer.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parseInt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(request.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getParameter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;delta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (NumberFormatException </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">e</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    response.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sendError</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(HttpServletResponse.SC_BAD_REQUEST);</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>7章ではこれが <strong>0行</strong>（型宣言に含まれる）になります。しかも6章の書き方では<strong>画面が増えるたびにこの7行が増えます</strong>。ここにあるのは「タイプ量が減った」以上の思想です——<strong>手続きで書いていたことを、宣言で書けるようにする。宣言されたことをどう実現するかは、フレームワークがやる。</strong> Spring はあなたの <code>int delta</code> という宣言を「仕様」として読んでいます。</p><p><strong>8. <code>@RequestParam(name = &quot;q&quot;, required = false)</code> の <code>required = false</code> を消すと何が起きるか。6章の <code>request.getParameter(&quot;q&quot;)</code> との違いは何か。</strong></p><p><strong><code>/app/list</code> が 400 Bad Request になります。</strong> <code>@RequestParam</code> の既定は <code>required = true</code> で、そのパラメータが無ければ Spring がメソッドを呼ぶ前に弾くからです。</p><table tabindex="0"><thead><tr><th></th><th>パラメータが無いとき</th></tr></thead><tbody><tr><td><code>request.getParameter(&quot;q&quot;)</code>（6章）</td><td><code>null</code> が返る。<strong>気づかない</strong></td></tr><tr><td><code>@RequestParam(&quot;q&quot;) String q</code></td><td><strong>400 Bad Request</strong>。呼ばれる前に弾かれる</td></tr><tr><td><code>@RequestParam(name=&quot;q&quot;, required=false) String q</code></td><td><code>null</code> が入る</td></tr></tbody></table><p>決定的な違いは、<strong>6章の <code>getParameter</code> は無ければ黙って <code>null</code> を返していた</strong>のに対し、<code>@RequestParam</code> は既定で必須を要求することです。<code>q</code> は絞り込みキーワードなので無いのが普通、だから <code>required = false</code>。一方 <code>create</code> の <code>code</code> / <code>name</code> / <code>quantity</code> はフォームが必ず送るので既定のままにしてあります。この挙動は <code>create_without_required_param_is_400</code> というテストで実測しています。</p><p><strong>9. <code>MockMvc</code> は何を偽物にし、何を本物のまま動かしているか。<code>ItemHttpIT</code> では検証できて <code>MockMvc</code> では検証できないものを2つ挙げよ。</strong></p><p>名前に &quot;Mock&quot; と付いていますが、<strong>Spring MVC そのものを偽物に差し替えるのではありません。偽物にするのは Servlet コンテナ（Tomcat）だけ</strong>です（このテストではさらに Repository も Fake にしています）。<code>HandlerMapping</code>・引数リゾルバ・型変換・<code>ItemController</code>・<code>ItemService</code> は<strong>全部本物が動いています</strong>。</p><p><code>MockMvc</code> では検証できないものは、<strong>(1) JSP のレンダリング結果（最終的な HTML の文字列）</strong>——<code>MockMvc</code> が検証できるのは「<code>&quot;item-list&quot;</code> というビュー名を返した」ところまでで、そのビューが本当に描画できるか、<code>${items}</code> が正しく展開されるかは分かりません——と、<strong>(2) コンテキストパスが正しく付くか</strong>——<code>MockMvc</code> にはコンテキストパスが無いので、<code>Location</code> が <code>/inventory/app/list</code> になるかは確認できません。これを確認しているのが <code>ItemHttpIT</code> の <code>endsWith(&quot;/app/list&quot;)</code> です。加えて <strong>Filter を通らない</strong>ので文字コードの確認も <code>ItemHttpIT</code> の担当です。</p><p>逆に、<code>view().name(...)</code> と <code>model().attributeExists(...)</code> は <strong>HTTP からは絶対に見えません</strong>。HTML を見れば「たぶん <code>item-list.jsp</code> が使われた」と推測はできても確認はできない——これが <code>MockMvc</code> にしかできない検証です。実測は <code>ItemControllerTest</code> 6件で <strong>0.579 秒</strong>、<code>ItemHttpIT</code> 6件で <strong>0.741 秒</strong>。<strong>両方要ります。どちらか一方では穴が空きます。</strong></p><p><strong>10. <code>ItemControllerTest</code> の <code>setUp()</code> で <code>FakeItemRepository</code> が刺さるのはなぜか。5章では、なぜ同じことができなかったか。</strong></p><p><code>ItemController</code> も <code>ItemService</code> も<strong>コンストラクタで依存を受け取る</strong>ので、上から下へ <code>new</code> を3回書くだけで組み立てられるからです。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">repository </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FakeItemRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ItemController controller </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ItemController</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ItemService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(repository));</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mockMvc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> MockMvcBuilders.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">standaloneSetup</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(controller).</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">build</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>特殊なライブラリも、リフレクションも、<code>static</code> への逃げも使っていません。核心はここです。</p><blockquote><p><strong>DI コンテナを使う設計にしておくと、テストでは DI コンテナ無しで組み立てられる。</strong></p></blockquote><p>5章でできなかったのは、<code>FrontController</code> が <code>private final ItemStore store = new ItemStore();</code> と自分で <code>new</code> していて渡す口が無かったからです。6章の <code>FrontController</code> も、<code>ContainerHolder</code> から <code>init()</code> で取りに行く形（サービスロケータ）だったので、テストから <code>new</code> しても <code>init()</code> が呼ばれず <code>service</code> が <code>null</code> のままでした。<strong>根本の原因は、Servlet を <code>new</code> するのが Tomcat であってあなたではないこと</strong>です。7章では Spring が Controller の生成に関与するので、この制約が消えました。なお <code>ItemController</code> には <code>jakarta.servlet</code> の import が1つもないので、HTTP が無くても呼べます（5章の「Servlet に依存しないクラスを分けるとテストが速くなる」の再適用）。もしフィールド注入を使っていたら、<code>new</code> してもフィールドが <code>null</code> のままなので、<strong>テストのために Spring を起動するかリフレクションでこじ開けるしかなくなります</strong>。だからこの教材はコンストラクタ注入しか使いません。</p><p><strong>11. Controller を全面的に書き換えたのに <code>ItemHttpIT</code> の6件が緑のままだったのは、テストが何を検証していたからか。</strong></p><p><strong>「利用者から見た振る舞い」だけを検証していたから</strong>です。6件が見ているのは、一覧が 200 で返り <code>ボールペン（黒）</code> が載っていること、<code>?q=コピー用紙</code> で絞り込めること、<code>?q=&lt;script&gt;</code> がエスケープされること、登録すると 302 で <code>/app/list</code> に飛ぶこと、在庫数が数値でないとエラーが出ること、知らない URL は 404 であること——<strong>実装がどう変わろうと変わってはいけない部分</strong>です。</p><p>この章では <code>FrontController</code> を削除し、<code>ContainerHolder</code> を削除し、ミニ DI コンテナを丸ごと削除し、<code>web.xml</code> を書き換え、<strong>Web の入口を別のフレームワークの Servlet に差し替えました</strong>。それでも6件は1件も落ちず、1文字も直していません。4章 → 5章 → 6章 → 7章と中身が3回変わっても、この6件は変わっていません。</p><p>これが「テストを書く」ことの本当の見返りです。5章では「手作業 6〜7分 → 0.44秒」という<strong>速さ</strong>の話をしましたが、それより大きい価値がこれです。</p><blockquote><p><strong>テストがあるから、大胆な作り直しができる。</strong></p></blockquote><p>テストが無ければ、<code>FrontController</code> を消して11項目を手で確認し、「たぶん大丈夫」と思い、3日後に絞り込みが壊れていたことに気づきます。だから「消していいのか分からない」古いコードが残り続けます。<strong>テストは、消す勇気を提供する道具でもあります。</strong> この章の完了時点で <code>Tests run: 19</code>（<code>ItemControllerTest</code> 6 + <code>ItemHttpIT</code> 6 + <code>ItemServiceTest</code> 7）が緑です。</p><p><strong>12. <code>ItemService</code> のアノテーションを <code>@Component</code> から <code>@Service</code> に変えても <code>ItemServiceTest</code> が無傷だったのはなぜか。</strong></p><p><strong>テストが <code>new ItemService(new FakeItemRepository())</code> と手で組み立てていて、アノテーションを一切見ていないから</strong>です。</p><blockquote><p><strong>アノテーションは「誰が組み立てるか」の話であって、「何をするか」の話ではない。</strong></p></blockquote><p><code>@Service</code> を消しても <code>new ItemService(...)</code> は同じように動きます（アプリのほうは <code>NoSuchBeanDefinitionException</code> で起動しなくなりますが）。この区別が付いていると、Spring のアノテーションを見て怯えなくなります。実際この章で <code>ItemService</code> に対してやったのは <code>@Component</code> → <code>@Service</code> に変え <code>@Inject</code> を消した（Spring 4.3 以降、コンストラクタが1つだけなら注釈は要らない）ことだけで、<code>search</code> も <code>register</code> も <code>changeQuantity</code> も<strong>中身は1文字も変えていません</strong>。</p><blockquote><p><strong>Spring は、あなたの設計を変えるものではありません。設計はあなたが6章で決めました。Spring がやるのは「組み立て」だけです。</strong></p></blockquote><p>もし6章で <code>ItemRepository</code> をインターフェースに切っていなかったら、この章は「Spring に合わせてクラスを書き直す章」になっていました。「Spring を使うために設計する」のではなく、「良い設計をしていたので Spring がそのまま乗る」——順序が逆であることを押さえてください。</p><p>なお、<code>@Service</code> / <code>@Repository</code> / <code>@Controller</code> は機能的にはほぼ <code>@Component</code> の別名ですが、<strong><code>@Repository</code> にだけは「DB のドライバ固有の例外を Spring 共通の例外に翻訳する」という機能的な違い</strong>があり、8章で JPA に置き換えたときに意味を持ちます（→ <a href="./../chapters/08-jpa">8章</a>）。</p><hr><h2 id="第8章" tabindex="-1">第8章 <a class="header-anchor" href="#第8章" aria-label="Permalink to &quot;第8章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/08-jpa">第8章 消えるデータ — JPA と永続化コンテキスト</a></p></blockquote><p><strong>1. 7章で登録したデータが再起動で消えたのはなぜか。8章で消えなくなったのは、データがどこへ移ったからか。</strong></p><p><code>InMemoryItemRepository</code> の実体が<strong>ただの <code>ArrayList</code></strong>、つまり <strong>JVM のヒープの上</strong>にあったからです。プロセスが死ねばデータも一緒に死にます。実測でも、<code>Z-999</code>（大事な商品）を登録して一覧に出たあと <code>docker compose restart tomcat</code> すると、何も出なくなりました。8章では置き場所が <strong>PostgreSQL のディスクの上</strong>、つまり<strong>プロセスの外</strong>に移りました。しかも <code>compose.yaml</code> の <code>volumes</code> で Docker ボリュームに載せているので、コンテナを作り直してもデータの実体は残ります。実測でも <code>P-001</code>（永続化テスト・42）は再起動後も残っていました。4章の <code>ItemStore</code> に書いた「<strong>8章で DB に置き換えるまでの暫定</strong>」というコメントの、約束の章です。</p><p><strong>2. この章で書き換えたクラスは何個か。<code>ItemService</code> と <code>ItemController</code> を1行も変えずに済んだのはなぜか。</strong></p><p><strong>1個だけ</strong>です（<code>InMemoryItemRepository</code> を削除し、<code>JpaItemRepository</code> を新しく書いた）。<code>ItemService</code> も <code>ItemController</code> も JSP も1行も変えていません。理由は、<strong><code>ItemService</code> が <code>InMemoryItemRepository</code> という名前を1度も書いていない</strong>からです。知っているのは <code>ItemRepository</code> インターフェースだけなので、実装が消えても影響を受けません。7章の <code>@ComponentScan</code> から見れば、<code>ItemRepository</code> を実装した <code>@Repository</code> の<strong>候補が <code>JpaItemRepository</code> 1つに変わっただけ</strong>です（両方残すと <code>NoUniqueBeanDefinitionException: expected single matching bean but found 2</code> になります）。</p><p>6章にはこう書いてありました——「実装が1つしかないうちからインターフェースを切るのは普通なら過剰だが、ここでは『差し替えられること』自体が目的である」。<strong>その代金の回収日がこの章です</strong>（→ <a href="./../chapters/06-di">6章</a>）。</p><p><strong>3. <code>Item</code> の <code>final</code> を外すことになった理由を、JPA がインスタンスを作る手順から説明せよ。</strong></p><p>JPA が DB の1行からオブジェクトを作る手順が、**「空の箱を作ってから、あとで値を入れる」**だからです。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>① SELECT で1行取ってくる（code=&quot;A-001&quot;, name=&quot;ボールペン（黒）&quot;, quantity=120）</span></span>\n<span class="line"><span>② new Item()   ← 引数なしコンストラクタを呼ぶ（この時点では全部 null / 0 の空っぽの箱）</span></span>\n<span class="line"><span>③ フィールドに値を入れる（リフレクション）</span></span></code></pre></div><p>②の時点でまだ値がなく、③で<strong>あとから</strong>入れるので、フィールドを <code>final</code> にはできません。ただし<strong>被害は最小にできます</strong>。この教材では getter は <code>public</code> のままにし、setter は <code>setQuantity</code> <strong>1つだけを package private</strong>（修飾子なし）にして、<code>setName</code> と <code>setCode</code> は<strong>そもそも作っていません</strong>。</p><blockquote><p><strong>JPA を使うと <code>final</code> は失われる。しかし「誰が変えられるか」は自分で決められる。JPA が要求するのは引数なしコンストラクタだけで、全部の setter ではない。</strong></p></blockquote><p>失ったのは、1章の <code>Item</code> が持っていた「不変オブジェクト」という性質です。不変であれば、3章で学んだ「複数スレッドが同時に触る」問題が<strong>原理的に起きません</strong>。それを1つ手放した、という自覚は持っておいてください。</p><p><strong>4. <code>protected Item() { }</code> を <code>private</code> にすると何が起きるか。なぜ <code>protected</code> なら良いのか。</strong></p><p><code>org.hibernate.InstantiationException: No default constructor for entity: com.example.inventory.Item</code> で落ちます。JPA の仕様が要求しているのは「<strong><code>public</code> または <code>protected</code> の引数なしコンストラクタ</strong>」だからです。<code>protected</code> である必要があるのは、Hibernate が遅延ロードのために<strong>エンティティのサブクラスを作ることがある</strong>からで、サブクラスは <code>private</code> のコンストラクタを呼べません。<code>protected</code> にしておけば、アプリのコードからは実質呼べず（同じパッケージからは呼べますが）、<strong>JPA からは呼べる</strong>という都合のよい位置に落ち着きます。</p><p><strong>5. <code>import javax.sql.DataSource;</code> は誤りではない。<code>jakarta.persistence.*</code> との違いを、「その API がどこに入っているか」から説明せよ。</strong></p><p>判定基準は1つです。</p><blockquote><p><strong>その API は JDK に入っているか。入っているなら <code>javax</code>、別途 JAR を足すものなら <code>jakarta</code>。</strong></p></blockquote><p><code>DataSource</code> は <strong>JDBC 仕様の一部</strong>で、JDBC は Jakarta EE ではなく <strong>Java SE（JDK）の一部</strong>です。Jakarta EE への移管対象になったのは Java <strong>EE</strong> の仕様だけだったので、JDBC は最初から対象外でした。だから <code>javax.sql.DataSource</code> は今も <code>javax</code> のままで正しく、<strong><code>jakarta.sql</code> というパッケージは存在しません</strong>。実際 <code>./mvnw -q dependency:tree | grep -i &quot;javax.sql&quot;</code> は<strong>何も出ません</strong>——どの JAR にも入っていない、つまり JDK にあるからです。一方 <code>jakarta.persistence.EntityManager</code> は <code>hibernate-core</code> が引き連れてきた <code>jakarta.persistence-api</code> の JAR に入っています。</p><table tabindex="0"><thead><tr><th>JDK（Java SE）側＝<code>javax</code> のまま</th><th>Jakarta EE 側＝<code>jakarta</code> に変わった</th></tr></thead><tbody><tr><td><code>javax.sql.DataSource</code> / <code>javax.sql.rowset</code></td><td><code>jakarta.servlet.*</code>（1章〜）</td></tr><tr><td><code>javax.crypto.*</code> / <code>javax.naming.*</code></td><td><code>jakarta.persistence.*</code>（8章）</td></tr><tr><td><code>javax.xml.parsers.*</code></td><td><code>jakarta.validation.*</code>（9章）/ <code>jakarta.mail.*</code>（10章）/ <code>jakarta.annotation.*</code></td></tr></tbody></table><p>つまり**「<code>javax</code> は古い」ではなく、「<code>javax</code> の Jakarta EE 部分だけが <code>jakarta</code> になった」**が正しい理解です（詳しくは <a href="./../appendix/javax-to-jakarta">javax 記事の読み替え表</a>）。</p><p><strong>6. 永続化コンテキストとは何か。いつ作られ、いつ捨てられるか。</strong></p><p><strong><code>EntityManager</code> が持っている「管理中のオブジェクトの箱」<strong>です。</strong><code>@Transactional</code> が付いたメソッドを呼ぶと箱が1つ作られ、メソッドが終わると箱は捨てられます。</strong> つまり <code>@Transactional</code> のメソッドと同じ寿命です。この箱は「1次キャッシュ」でもありますが、アプリ全体で共有される2次キャッシュとは別物で、<strong>トランザクションごとに作られて捨てられます</strong>。だから <code>find</code> を2回呼んで SQL が1回で済むのも、<code>@Transactional</code> のメソッドを抜けるまでの話です。</p><p>この章の主張は、ここに集約されます——<strong>JPA が難しいのは SQL のせいではなく、「いつ DB に書かれるか」があなたの書いたコードから見えないから</strong>です。</p><p><strong>7. <code>em.persist(item)</code> を呼んだ瞬間、DB に INSERT は発行されているか。されていないなら、いつ発行されるか。</strong></p><p><strong>発行されていません。</strong> <code>persist</code> がやるのは「新しいエンティティを箱（永続化コンテキスト）に入れて管理下に置く」ことだけです。INSERT が実際に出るのは <strong>flush のとき</strong>——明示的に <code>em.flush()</code> を呼ぶか、<strong>コミット直前に自動で走る</strong>ときです。<code>@Transactional</code> のメソッドは ①BEGIN → ②メソッド実行 → ③flush してコミット、という流れなので、<strong>メソッドを抜けるときに初めて DB へ書かれます</strong>。テストの中で <code>repository.add(...)</code> の直後にはまだ INSERT が出ておらず、<code>em.flush()</code> を呼んだ瞬間に <code>insert into items ...</code> が出ることを実測で確かめています。<code>setQuantity</code> も同じで、その場では SQL を出しません。</p><p><strong>8. <code>update</code> 文を書いていないのに UPDATE が発行される仕組みを説明せよ。Hibernate は何と何を比較しているか。</strong></p><p>**変更検知（dirty checking）**です。比較しているのは「<strong>取得時のスナップショット</strong>」と「<strong>現在の値</strong>」です。</p><p><code>em.find</code> で1行取ってくるとき、Hibernate は箱に入れると同時に<strong>取ってきた瞬間の値を別に控えます</strong>（<code>{code:&quot;C-100&quot;, name:&quot;ステープラー&quot;, quantity: 7}</code>）。<code>item.setQuantity(10)</code> はただの Java の setter で SQL は1文も出ません。メソッドを抜けるとき、flush が箱の中身とスナップショットを1件ずつ比べ、<code>quantity: 10 ≠ 7</code> なので UPDATE を発行します。</p><blockquote><p><strong>JPA は「あなたが <code>update</code> と書いたから」UPDATE を出すのではありません。「取ってきたときの値と、今の値が違うから」UPDATE を出します。</strong></p></blockquote><p>そもそも <strong><code>em.update(...)</code> というメソッドは JPA に存在しません。</strong> 実測では <code>code=C-100&amp;delta=3</code> を POST するとログに <code>select ... where i1_0.code=?</code> に続いて <code>update items set name=?,quantity=? where code=?</code> が出て、DB の値が <strong>7 → 10</strong> になりました。魔法に見えるのは、<strong>スナップショットが目に見えないから</strong>です。</p><p><strong>9. <code>update items set name=?,quantity=? where code=?</code> に、変更していない <code>name</code> が入っているのはなぜか。</strong></p><p><strong>Hibernate が既定で全カラムを UPDATE する</strong>からです。理由は性能で、「どのカラムが変わったか」ごとに違う SQL を作ると<strong>組み合わせの数だけ SQL 文が必要になり、DB 側のプリペアドステートメントのキャッシュが効かなくなります</strong>。全カラム更新なら SQL は常に1本で済みます。変わっていないカラムに変わっていない値を書き込んでいるだけなので、結果は同じです。<code>@DynamicUpdate</code> を付ければ変わったカラムだけの SQL にできますが、巨大なテーブルでない限り既定のままが有利です。<strong>ログに <code>name=?</code> が出ても異常ではありません。</strong></p><p><strong>10. 同じ ID で <code>find</code> を2回呼んでも SELECT が1回しか出ないのはなぜか。2回目に返るのは「値が等しい別のインスタンス」か「同じインスタンス」か。テストのどのアサーションでそれを確かめたか。</strong></p><p><strong>永続化コンテキスト（＝1次キャッシュ）に、1回目で取ってきたインスタンスがすでに入っているから</strong>です。2回目の <code>find</code> は箱を見て「ある」と分かるので、<strong>SQL を出さずにそれを返します</strong>。返るのは「値が等しい別のインスタンス」ではなく、<strong>まったく同じ1個のインスタンス</strong>です。</p><p>確かめたのは <code>second_find_hits_persistence_context</code>（「★同じ ID で find を 2 回呼んでも SELECT は 1 回しか出ない」）の、この2行です。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertEquals</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(afterFirst, afterSecond, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;2 回目の find で SQL が発行された&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertSame</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(first, second, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;別のインスタンスが返っている&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><code>assertEquals</code> は値が等しいか（<code>equals</code>）、**<code>assertSame</code> は同じインスタンスか（<code>==</code>）**を見ます。SQL の本数は Hibernate の <code>Statistics</code> の <code>getPrepareStatementCount()</code> で数えました。</p><p>なぜこれが重要か。<strong>変更検知が壊れないから</strong>です。</p><blockquote><p><strong>永続化コンテキストは、キャッシュであると同時に「同一性の保証」でもある。1つのトランザクションの中で、同じ ID のエンティティは常に同じインスタンスである。</strong></p></blockquote><p>もし <code>find</code> のたびに別インスタンスが返ってきたら、<code>a.setQuantity(50)</code> した後に <code>b</code> を見ても 50 にならず、「どっちの変更を保存すればいいのか」が決まりません。</p><p><strong>11. <code>@Transactional</code> を外すと、<code>persist</code> と <code>setQuantity</code> はそれぞれどうなるか。</strong></p><table tabindex="0"><thead><tr><th>何が起きるか</th><th>なぜか</th></tr></thead><tbody><tr><td><strong><code>persist</code> しても DB に何も入らない</strong></td><td>トランザクションが無いので、flush もコミットもされない</td></tr><tr><td><strong><code>setQuantity</code> しても UPDATE が出ない</strong></td><td>変更検知は「コミット時」に働く。コミットが無い</td></tr><tr><td>エラーになることすらある</td><td><code>TransactionRequiredException: No EntityManager with actual transaction available</code></td></tr></tbody></table><p><strong>一番怖いのは1つ目</strong>です。<strong>例外が飛ばないので、「保存したつもり」で先へ進めてしまいます。</strong> 画面上は成功したように見えて、DB を見ると何も入っていません。だからこの章では、要所で <code>psql</code> を叩いて DB を直接見て確認しています。</p><p><strong>12. テストで <code>@Autowired private JpaItemRepository repository;</code> と書くと落ちる理由を、プロキシの図を使って説明せよ。なぜ <code>ItemRepository</code> なら受け取れるのか。</strong></p><p>実際にこれで<strong>6件とも Error</strong> になり、こう出ました。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BeanNotOfRequiredTypeException: Bean named &#39;itemRepository&#39; is expected to be of type</span></span>\n<span class="line"><span>&#39;com.example.inventory.JpaItemRepository&#39; but was actually of type &#39;jdk.proxy2.$Proxy71&#39;</span></span></code></pre></div><p><code>jdk.proxy2.$Proxy71</code> は<strong>あなたが書いていないクラス</strong>で、Spring が実行時に作ったものです。<code>@Transactional</code> の BEGIN / COMMIT は <code>JpaItemRepository</code> の中に1行も書いていないのに実行されます——<strong>Spring があなたのクラスを「包んだ」別のオブジェクトを作り、コンテナに登録しているのはその外側のプロキシだから</strong>です。そして <strong>JDK 動的プロキシはインターフェースしか実装できません</strong>。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>            ItemRepository（interface）</span></span>\n<span class="line"><span>                  ▲               ▲</span></span>\n<span class="line"><span>                  │ implements    │ implements</span></span>\n<span class="line"><span>        JpaItemRepository    jdk.proxy2.$Proxy71</span></span>\n<span class="line"><span>        （あなたが書いた）     （Spring が実行時に作った）</span></span>\n<span class="line"><span>              ★この2つの間に継承関係は無い</span></span></code></pre></div><p>だから具象型では受け取れず、<strong><code>@Autowired private ItemRepository repository;</code> とインターフェースで受ければ通ります</strong>。これが <strong>AOP</strong>（本体のコードに触らずに、その前後に共通処理を差し込む仕組み）の入口で、7章の JAR 一覧にあった <code>spring-aop</code> の正体です。<strong>4章の Filter がリクエストを包むなら、プロキシはメソッド呼び出しを包みます。</strong></p><blockquote><p><strong><code>@Transactional</code> が付いたクラスは、コンテナの中では別のクラスになっている。だから具象型ではなくインターフェースで受け取る。</strong></p></blockquote><p>6章で「インターフェースで受け取る」設計にしたことの、<strong>思わぬ2つ目の見返り</strong>です。</p><p><strong>13. <code>hbm2ddl.auto</code> の <code>create</code> と <code>update</code> の違いは何か。なぜ <code>update</code> も本番では使わないのか。代わりに何を使うか。</strong></p><p><code>create</code> は <strong>DROP してから CREATE</strong>——起動のたびにデータが全部消えます。実測でもログに <code>drop table if exists items cascade</code> が出ました。<strong>本番の DB でこれが走ったら事故です。</strong> <code>update</code> は足りないテーブル・カラムを足すだけで既存データは消しません。</p><p>それでも <code>update</code> を本番で使わないのは、<strong>やらないことが多すぎる</strong>からです。テーブルを消さない、カラムを消さない（消したカラムは残り続ける）、カラムの型を変えない、既存データを新しい形に移さない。つまり<strong>スキーマを変えたときに「今あるデータをどうするか」を誰も決めていません</strong>（<code>quantity</code> を <code>int</code> から <code>bigint</code> に、<code>name</code> を 100 文字から 200 文字に、といった変更が典型です）。</p><p>代わりに使うのは <strong>Flyway や Liquibase</strong> です。スキーマの変更履歴を SQL として自分で書き、順番に適用します。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>V1__create_items.sql        CREATE TABLE items (...)</span></span>\n<span class="line"><span>V2__add_category.sql        ALTER TABLE items ADD COLUMN category varchar(50)</span></span>\n<span class="line"><span>V3__widen_name.sql          ALTER TABLE items ALTER COLUMN name TYPE varchar(200)</span></span></code></pre></div><p><strong>「誰が、いつ、何を変えたか」がファイルとして残り、git で追えます。</strong> <code>hbm2ddl.auto=update</code> には、この履歴がありません。この教材では名前を挙げるだけにしますが、<strong>「本番では使わない」という一線だけは引いておいてください</strong>（この教材の設定は、本番が <code>update</code>、テストが <code>create-drop</code> です）。</p><p><strong>14. <code>ItemServiceTest</code> の7件が1文字も変えずに緑のままだったのはなぜか。7章で <code>ItemHttpIT</code> が緑のままだったのと、守られた境界は同じか違うか。</strong></p><p><code>ItemServiceTest</code> が <strong><code>FakeItemRepository</code> を使っていて、DB がまったく関係ないから</strong>です。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">FakeItemRepository repository </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> FakeItemRepository</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ItemService service </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ItemService</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(repository);</span></span></code></pre></div><p>この章では <code>InMemoryItemRepository</code> を削除し、保管先をメモリから PostgreSQL に変え、<code>Item</code> をエンティティ化して <code>final</code> を外し、トランザクション管理を導入しました——<strong>データ層を丸ごと入れ替えました</strong>。それでも境界の上（<code>ItemService</code> / <code>ItemController</code> / それらのテスト）は無傷でした。</p><p>守られた境界は <strong>7章とは別方向</strong>です。7章は「<strong>入口</strong>を全部作り直しても振る舞いのテストは落ちない」、8章は「<strong>データ層</strong>を全部作り直しても業務ロジックのテストは落ちない」。</p><blockquote><p><strong>境界線を1本引いておくと、その両側は独立して作り直せる。</strong></p></blockquote><p>6章でインターフェースを切ったコストは、<strong>7章と8章の2回、別々の方向から回収されました。</strong></p><p><strong>15. <code>JpaItemRepositoryTest</code>（2.155秒）と <code>ItemServiceTest</code>（0.008秒）の差は何倍か。業務ルールのテストをどちらの側に置くべきか、理由とともに答えよ。</strong></p><p>クラス全体で <strong>2.155 ÷ 0.008 ≒ 270倍</strong>（1件あたりでは約 0.36 秒 対 約 0.0011 秒で<strong>約 320倍</strong>）です。しかも <code>JpaItemRepositoryTest</code> が使っているのは **H2（インメモリの DB）**であって、ディスクに書く PostgreSQL ならもっと遅くなります（2.155 秒の大半は Spring のコンテナ起動と Hibernate の初期化です）。</p><p>依存が増えるほど遅くなる、という順に並ぶのが分かります。</p><table tabindex="0"><thead><tr><th>テスト</th><th>件数</th><th>時間</th><th>依存</th></tr></thead><tbody><tr><td><code>ItemServiceTest</code></td><td>7</td><td><strong>0.008 秒</strong></td><td>なし（Fake だけ）</td></tr><tr><td><code>ItemControllerTest</code></td><td>6</td><td>0.475 秒</td><td>Spring MVC</td></tr><tr><td><code>ItemHttpIT</code></td><td>6</td><td>0.876 秒</td><td><strong>本物の Tomcat</strong></td></tr><tr><td><code>JpaItemRepositoryTest</code></td><td>6</td><td><strong>2.155 秒</strong></td><td>H2 + JPA</td></tr></tbody></table><p><strong>業務ルールのテストは、絶対に一番速い側（<code>ItemServiceTest</code>）に置いてください。</strong>「在庫がマイナスにならない」を DB 込みでテストすると、ルールが10個になったときテストが数十秒になります。<strong>業務ルールが増えるのは Service 層です。そこが一番速いことに意味があります。</strong> 5章から一貫した方針——<strong>速いテストで大半を確認し、遅いテストは「経路が通っているか」だけに絞る</strong>——のとおりです。この章の完了時点で <code>Tests run: 25</code> が緑です。</p><hr><h2 id="第9章" tabindex="-1">第9章 <a class="header-anchor" href="#第9章" aria-label="Permalink to &quot;第9章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/09-validation">第9章 壊れた入力 — Bean Validation</a></p></blockquote><p><strong>1. アノテーションで書ける検証と、書けない検証を分ける基準を1文で述べよ。このアプリで「書けない検証」はどれか。</strong></p><blockquote><p><strong>アノテーションで書けるのは「その値だけを見て決まること」だけ。</strong></p></blockquote><p>判定するときの問いは「<strong>この値だけを持って、他に何も見ずに、答えを出せるか?</strong>」です。「コードが空でない」「20文字以内」「英数字とハイフンだけ」「在庫数が 0 以上」は、<code>&quot;A-001&quot;</code> という文字列だけで判定でき、<strong>世界のどこにも問い合わせなくてよい</strong>ので <code>@NotBlank</code> / <code>@Size</code> / <code>@Pattern</code> / <code>@Min</code> で書けます。</p><p>このアプリで<strong>書けない検証は「コードの重複チェック」<strong>です。「そのコードがすでに使われているか」は <strong>DB を見ないと決まりません</strong>。昨日は OK でも今日はダメかもしれない——<strong>同じ入力でも答えが変わる</strong>からです。アノテーションは</strong>クラス定義に埋め込まれた固定の宣言</strong>なので、実行時に DB へ問い合わせるような<strong>状況依存の判断は原理的に表現できません</strong>。だからこれは <code>binding.rejectValue(&quot;code&quot;, &quot;duplicated&quot;, ...)</code> としてコードで書きます。「在庫を減らして 0 未満にならない」も同じ理由で書けません（<code>ItemService</code> の仕事です）。</p><blockquote><p><strong>入力の形式は「宣言」に、業務の判断は「コード」に。この2つは同じ場所に書いてはいけない。</strong></p></blockquote><p><strong>2. <code>Item</code>（エンティティ）に <code>@NotBlank</code> を付けず、<code>ItemForm</code> を別に作った理由を3つ挙げよ。</strong></p><p>(1) <strong>DB から読んだ <code>Item</code> にも制約がかかってしまう</strong>——過去に登録された21文字のコードが、読むだけで検証対象になります。(2) <strong>10章の XML 取込にも同じ制約がかかる</strong>——画面のルールと連携のルールは同じとは限りません。(3) <strong><code>quantity</code> を <code>Integer</code> にする必要が出る</strong>——DB の <code>NOT NULL</code> 列なのに <code>null</code> を許す型になってしまいます。加えて (4) <strong>「フォームの都合」がエンティティに漏れ、画面を1つ足すたびにエンティティが揺れます。</strong></p><p>根っこにあるのは、2つのクラスの<strong>寿命も責務も違う</strong>という事実です。</p><table tabindex="0"><thead><tr><th></th><th><code>ItemForm</code></th><th><code>Item</code></th></tr></thead><tbody><tr><td>生まれる</td><td>HTTP リクエストが来た瞬間</td><td>検証を通ったとき / DB から読んだとき</td></tr><tr><td>死ぬ</td><td>レスポンスを返した瞬間</td><td>DB の行を消したとき（アプリより長生き）</td></tr><tr><td>中身</td><td>人間が打った、まだ信用できない文字列</td><td>正しいことが保証された値</td></tr><tr><td><code>quantity</code></td><td><code>Integer</code>（未入力なら null がありうる）</td><td><code>int</code>（null はありえない）</td></tr></tbody></table><blockquote><p><strong>エンティティは「DB の行」を表すものです。画面の都合で揺らしてはいけません。</strong></p></blockquote><p>8章で <code>Item</code> に <code>@Entity</code> と <code>@Id</code> を付けたとき、あのクラスは**「DB の世界の住人」になりました**（→ <a href="./../chapters/08-jpa">8章</a>）。</p><p><strong>3. <code>ItemForm.quantity</code> が <code>int</code> ではなく <code>Integer</code> なのはなぜか。7章の <code>@RequestParam(&quot;delta&quot;) int delta</code> が <code>int</code> でよかったのは、何が違うからか。</strong></p><p><strong>「未入力」を <code>null</code> として受け取り、<code>@NotNull</code> で検出するため</strong>です。<code>int</code> にすると未入力が <code>0</code> になり、<strong>「入力していない」と「0 と入力した」を区別できません</strong>。在庫 0 の商品は実在します（一覧の <code>B-011 A3 コピー用紙</code> がまさに 0 です）——つまり <code>int</code> にすると<strong>打ち忘れが 0 として保存されます</strong>。また、空文字を <code>int</code> に変換しようとすると型変換エラーになり、「未入力」なのに「形式が正しくありません」と言われてしまいます。</p><p>7章の <code>delta</code> が <code>int</code> でよかったのは、<strong>hidden フィールドが必ず値を送るから</strong>です。「必ず来る値」と「来ないかもしれない値」で型を使い分けます。</p><table tabindex="0"><thead><tr><th>場面</th><th>型</th><th>未入力のとき</th></tr></thead><tbody><tr><td><code>@RequestParam(&quot;delta&quot;) int</code>（7章）</td><td><code>int</code></td><td><strong>400 Bad Request</strong></td></tr><tr><td><code>ItemForm.quantity</code>（9章）</td><td><code>Integer</code></td><td><code>null</code> → <code>@NotNull</code> が拾う</td></tr></tbody></table><p>そして <code>delta</code> が壊れているのは「URL を手で叩いた」か「バグ」なので 400 でよいのに対し、<code>quantity</code> は<strong>人間がキーボードで打ち間違えた</strong>値です。<strong>人間が打つ欄で 400 を返してはいけません</strong>——打ち直させたいからです。</p><p><strong>4. <code>BindingResult</code> を検証対象の直後に書かなければならないのはなぜか。離すと何が起きるか。</strong></p><p>離すと <code>IllegalStateException: An Errors/BindingResult argument is expected to be declared immediately after the model attribute...</code> で <strong>500 になります</strong>。<code>Model</code> を1つ挟んだだけで壊れます。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Valid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ItemForm form, BindingResult binding, Model model)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★動く</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> String </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Valid</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ItemForm form, Model model, BindingResult binding)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★500</span></span></code></pre></div><p>なぜこんな仕様なのかというと、<strong>メソッドに検証対象が2つあってもよいから</strong>です。<code>@Valid ItemForm</code> と <code>@Valid UserForm</code> が並ぶとき、<code>BindingResult</code> が2つあると「どちらがどちらの結果か」を決める規則が要ります。Spring はそれを「<strong>直前の引数のもの</strong>」と決めました。<strong>引数の名前ではなく、位置で対応を表現している</strong>わけです。</p><blockquote><p><strong>Java のメソッド引数は、順序に意味を持たせない設計が普通です。<code>BindingResult</code> は、その例外です。知らないと、まず気づけません。</strong></p></blockquote><p><strong>5. <code>BindingResult</code> を引数に書き忘れると、検証に失敗したときどうなるか。</strong></p><p><strong>Spring が例外を投げてリクエストが 400 になります。</strong> フォームには戻りません。「検証エラーを自分で処理する気がないなら、こちらで弾く」という挙動です。裏を返せば、</p><blockquote><p><strong><code>BindingResult</code> を書くことが、「エラーは自分で扱います」という宣言です。</strong></p></blockquote><p><strong>6. <code>@ModelAttribute(&quot;itemForm&quot;)</code> が、<code>@RequestParam</code> × 3 に加えてやっていることは何か。8章の Controller から消えた3行は何か。</strong></p><p>加えてやっているのは、<strong>組み立てたオブジェクトを Model にも置くこと</strong>です。<code>@RequestParam</code> が「1つのパラメータを1つの引数に入れる」のに対し、<code>@ModelAttribute</code> は ①<code>new ItemForm()</code> ②③④ 名前が一致する setter を呼ぶ（型変換込み）⑤ <strong><code>model.addAttribute(&quot;itemForm&quot;, form)</code></strong> までを自動でやります。</p><p>だから8章にあったこの3行が消えました。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addAttribute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, code);        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★9章では不要</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addAttribute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, name);        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★9章では不要</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">model.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addAttribute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;quantity&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, quantity);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★9章では不要</span></span></code></pre></div><p><code>return &quot;item-form&quot;;</code> した時点で、JSP は <code>${itemForm.code}</code> で入力値を読めます。<strong>「入力値を保持する」が、書かずに実現されました。</strong> なお <code>@GetMapping(&quot;/new&quot;)</code> にも <code>@ModelAttribute(&quot;itemForm&quot;) ItemForm form</code> を書くのは、<strong>初回表示のときに空の <code>ItemForm</code> を Model に置くため</strong>です。無いとフォームタグが <code>Neither BindingResult nor plain target object for bean name &#39;itemForm&#39; available as request attribute</code> で例外を投げます。</p><p><strong>7. <code>@Min(message = &quot;在庫数は 0 以上で...&quot;)</code> と書いてあるのに、<code>quantity=abc</code> のときに英語の内部メッセージが出たのはなぜか。どの部品が出したメッセージか。</strong></p><p>出したのは **Spring のデータバインダ（型変換）**であって、Bean Validation ではないからです。処理は2段階で進みます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>① Spring のデータバインダ: &quot;abc&quot; を Integer に変換 → 失敗</span></span>\n<span class="line"><span>     → BindingResult に typeMismatch エラーを入れる</span></span>\n<span class="line"><span>     → ★ setQuantity は呼ばれない（quantity は null のまま）</span></span>\n<span class="line"><span>     → ★ ここは Spring の仕事。@Min も @NotNull も無関係</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>② Bean Validation（@Valid）: 型変換に失敗した項目は検証をスキップする</span></span>\n<span class="line"><span>     → ★ だから @NotNull の「在庫数を入力してください」も出ない</span></span></code></pre></div><p>画面に出たのは①のメッセージ、<code>Failed to convert property value of type java.lang.String to required type java.lang.Integer for property quantity; For input string: &quot;abc&quot;</code> です。<strong><code>@Min(message = &quot;...&quot;)</code> に日本語を書いても①には届きません。①が動くのは <code>@Valid</code> が始まる前だからです。</strong></p><blockquote><p><strong>アノテーションを付けただけでは、すべてのメッセージが日本語になるわけではない。型変換のエラーは Bean Validation の管轄外なので、別の場所で日本語にする。</strong></p></blockquote><p>その「別の場所」が <code>messages.properties</code> です。</p><p><strong>8. <code>typeMismatch</code> のキーが4段階で探索される順を答えよ。<code>typeMismatch</code>（最も広いキー）も定義しておく理由は何か。</strong></p><p>Spring は型変換に失敗すると、<strong>上から順に4つのキーを探し、最初に見つかったものを使います</strong>。</p><table tabindex="0"><thead><tr><th>順</th><th>キー</th><th>適用範囲</th></tr></thead><tbody><tr><td>1</td><td><code>typeMismatch.itemForm.quantity</code></td><td><strong><code>itemForm</code> の <code>quantity</code> だけ</strong></td></tr><tr><td>2</td><td><code>typeMismatch.quantity</code></td><td>どのフォームでも <code>quantity</code> という名前</td></tr><tr><td>3</td><td><code>typeMismatch.java.lang.Integer</code></td><td><code>Integer</code> への変換すべて</td></tr><tr><td>4</td><td><code>typeMismatch</code></td><td><strong>すべての型変換エラー</strong></td></tr></tbody></table><p><code>itemForm</code> の部分は <code>@ModelAttribute(&quot;itemForm&quot;)</code> で付けた名前なので、<strong>名前を変えたらこのキーも変えないと効かなくなります</strong>。</p><p>4番目も定義しておくのは、<strong>「網を1枚敷いておく」ため</strong>です。10章以降フォームの項目は増えます。新しく <code>Integer</code> の項目を足したときに <code>typeMismatch.itemForm.○○</code> を書き忘れると、また英語が出ます。網があれば「入力の形式が正しくありません」に落ちます。英語の内部メッセージが画面に出るのは単に不親切なだけでなく、<strong><code>java.lang.Integer</code> や <code>property quantity</code> といった内部構造の情報を、見ず知らずの利用者に見せている</strong>ことにもなります。</p><p><strong>9. <code>&lt;form:input path=&quot;quantity&quot;/&gt;</code> が、8章の <code>value=&quot;&lt;c:out value=&#39;${quantity}&#39;/&gt;&quot;</code> に対して肩代わりしたことを3つ挙げよ。</strong></p><p>(1) <strong>入力値の保持</strong>——<code>path</code> から <code>modelAttribute</code> の <code>getQuantity()</code> を呼んで <code>value</code> を埋めます。(2) <strong>エスケープ</strong>——<code>value</code> の中身を自動的に HTML エスケープするので、<code>&lt;c:out&gt;</code> で包む必要がありません（<code>&lt;form:errors&gt;</code> も同様で、<code>rejectValue</code> の第3引数に入力値を混ぜても <code>&amp;lt;</code> に変換されます）。(3) <strong>名前の一致</strong>——<code>path=&quot;quantity&quot;</code> から <code>name=&quot;quantity&quot;</code> と <code>id=&quot;quantity&quot;</code> が自動生成されるので、<strong><code>name</code> と <code>path</code> が一致していないと動かない、という間違いが構造的に消えます</strong>。</p><p>加えて、エラー表示も <code>&lt;c:if&gt;</code> + <code>&lt;c:forEach&gt;</code> で上部に一括表示していたものが、<code>&lt;form:errors path=&quot;code&quot;/&gt;</code> による<strong>項目ごとの振り分け</strong>に変わりました。</p><p><strong>10. 型変換に失敗して <code>quantity</code> が <code>null</code> なのに、画面の入力欄に <code>abc</code> が残っていたのはなぜか。</strong></p><p><strong><code>BindingResult</code> が「変換前の生の値（rejectedValue）」も保持していて、<code>&lt;form:input&gt;</code> がそちらを出しているから</strong>です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>BindingResult</span></span>\n<span class="line"><span>  ├─ フィールドエラー: quantity は変換に失敗した</span></span>\n<span class="line"><span>  └─ ★拒否された値（rejectedValue）: &quot;abc&quot;     ← &lt;form:input&gt; はこちらを出す</span></span></code></pre></div><p>8章の <code>value=&quot;&lt;c:out value=&#39;${quantity}&#39;/&gt;&quot;</code> は Controller で <code>model.addAttribute</code> した値を出すだけなので、<strong>Controller まで来なかった値（400 で弾かれた値）はそもそも出しようがありませんでした。</strong></p><blockquote><p><strong>「打ち間違えた値を、打ち間違えたまま画面に戻す」は、入力フォームの品質を決める地味だが決定的な要件です。</strong></p></blockquote><p><code>3O</code>（数字の 3 とアルファベットの O）と打ち間違えたとき、欄が空になって全部打ち直しのフォームと、<code>3O</code> が残っていて1文字直せばいいフォームでは、使い心地がまったく違います。</p><p><strong>11. <code>binding.rejectValue(&quot;code&quot;, &quot;duplicated&quot;, &quot;...&quot;)</code> の第1引数が、8章の <code>List&lt;String&gt;</code> に無かった情報とは何か。それがあると何ができるようになるか。</strong></p><p><strong>「どの項目のエラーか」という情報</strong>です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>8章:  [&quot;そのコードはすでに使われています: N-1&quot;]</span></span>\n<span class="line"><span>       └─ 文字列だけ。どの項目のものか、機械には分からない</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>9章:  code → [&quot;そのコードはすでに使われています: N-1&quot;]</span></span>\n<span class="line"><span>       └─ ★項目の名前を持っている。だから項目の横に出せる</span></span></code></pre></div><p>8章の形では、項目の情報が<strong>日本語の文の中にしか入っていません</strong>。項目が3つならまだ目で追えますが、10個になったら破綻します。第1引数があることで、<code>&lt;form:errors path=&quot;code&quot;/&gt;</code> の位置に<strong>その項目のエラーだけを出せる</strong>ようになります。実測でも、空の3項目を送ると <code>id=&quot;code.errors&quot;</code> <code>id=&quot;name.errors&quot;</code> <code>id=&quot;quantity.errors&quot;</code> という3つの別々の場所にメッセージが出て、<strong>エラーが項目に紐づいていることが HTML の構造そのものに現れています</strong>。</p><p>テストでも効きます。<code>model().attributeHasFieldErrors(&quot;itemForm&quot;, &quot;code&quot;)</code> と書けるのは9章からで、8章までは <code>hasItem(&quot;コードを入力してください&quot;)</code> のような文字列一致しか書けず、<strong>メッセージの語尾を「入力して下さい」に直しただけでテストが落ちました</strong>。</p><blockquote><p><strong>テストは「文字列」ではなく「構造」で書けるほうが、壊れにくくなります。</strong></p></blockquote><p><strong>12. <code>!binding.hasFieldErrors(&quot;code&quot;)</code> を先に確認してから重複を調べる理由を2つ挙げよ。</strong></p><p>(1) <strong>無意味な DB アクセスを避けるため。</strong> コードが空文字なら重複を調べるまでもなく登録できないので、<code>isCodeTaken(&quot;&quot;)</code> を呼んでも DB に無駄な SELECT が1回飛ぶだけです。(2) <strong>エラーメッセージが二重に出るのを防ぐため。</strong> チェックしないと、こうなります。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>コード: [        ]  コードを入力してください</span></span>\n<span class="line"><span>                    そのコードはすでに使われています:</span></span></code></pre></div><p>2つ目は明らかに間違った指摘です。<strong>空文字が「使われている」わけがありません。</strong></p><blockquote><p><strong>検証には順序があります。「形式が正しい」ことを確認してから、「業務上正しい」かを見る。</strong></p></blockquote><p>この順序を Spring は自動ではやってくれません。<strong>あなたが <code>if</code> で書きます。それでよいのです。業務の判断は、業務を知っている人が書くものだからです。</strong></p><p><strong>13. 5章で書いた <code>ItemHttpIT</code> が、9章の変更で落ちたのはなぜか。そのとき「テストを実装に合わせて書き換える」を選ばなかったのはなぜか。</strong></p><p>落ちたのは <code>create_rejects_non_numeric_quantity</code> です。このテストが検証していたのは「<strong>在庫数は数値で入力してください</strong>」という文字列が画面に出ることでした。9章で検証の仕組みを入れ替えた結果、そのメッセージが <code>Failed to convert property value of type java.lang.String to ...</code> に変わったので落ちました。<strong>5章に書いたテストが、4章分あとの変更を検知した</strong>わけです。</p><p>テストが赤くなったとき、可能性は2つあります。<strong>(A) 実装を壊した</strong>→実装を直す。<strong>(B) 仕様を意図的に変えた</strong>→テストを直すか、仕様を元に戻す。今回は (B) で、選んだのは「<strong>仕様を元に戻す</strong>」でした。「在庫数は数値で入力してください」という日本語メッセージは、<strong>変えるつもりがなかった</strong>からです。<code>messages.properties</code> に <code>typeMismatch.itemForm.quantity</code> を足したら、<strong>テストのコードを1行も変えずに緑に戻りました</strong>。</p><p>やらなかったのは、こう書き換えることです。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assertTrue</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(body.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">contains</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Failed to convert property value&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ★これはやらない</span></span></code></pre></div><blockquote><p><strong>テストを実装に合わせて書き換えるのは、最も危険な操作です。</strong> それをやると、テストは「実装が今こうなっている」ことしか言わなくなり、<strong>「こうあるべき」を守る力を失います。</strong></p><p><strong>テストが赤くなったとき、最初に問うのは「テストが間違っているか」ではなく「なぜ仕様が変わったか」です。</strong></p></blockquote><p>もしテストが無かったら、<code>curl</code> で正常系だけ確認して「よし、できた」と思い、<strong>数か月後に誰かが在庫数に <code>abc</code> と打って、画面に英語のスタックトレース由来のメッセージが出る</strong>ところまで気づけません。壊れたのは「特定の1つの異常系のメッセージ」だけなので、手で確認していたら、まず見つかりません。</p><p><strong>14. <code>ItemServiceTest</code> が7件から5件に減ったのは、機能が減ったからか。</strong></p><p><strong>違います。責任が移ったからです。</strong> 消えた2件は「空文字を渡したら」「数値でない文字列を渡したら」という<strong>入力の形式検証</strong>で、それは Bean Validation（<code>ItemFormValidationTest</code> の8件）に移りました。<code>ItemServiceTest</code> に残った5件は業務上の判断——絞り込みのルール、キーワード無しは全件、登録できる、<code>isCodeTaken</code> が重複を検出する（★新規）、在庫がマイナスにならない——だけです。</p><p><strong>合計はむしろ 7件 → 13件に増えています。</strong> 検証のテストは濃くなりました。しかも移った先のほうが速く、<code>ItemFormValidationTest</code> は <code>FakeItemRepository</code> すら使いません（8件で 0.035 秒）。</p><blockquote><p><strong>テストの数は、その章の到達点を測る指標ではありません。「どのテストが、どの責任を守っているか」が一致していることが重要です。</strong></p></blockquote><p>もし <code>ItemServiceTest</code> に「空文字を弾く」テストが残っていたら、それは <strong><code>ItemService</code> が検証の責任を持っている</strong>ということになり、この章でやった線引きと矛盾します。なお <code>ItemService.register</code> は9章で1行になりました——<strong><code>@Valid</code> を通っていない <code>ItemForm</code> はこのメソッドに到達できない</strong>ので、奥のコードを防御的に書かなくてよくなったからです。</p><p><strong>15. <code>&lt;input type=&quot;number&quot;&gt;</code> を付けてもサーバ側の検証が必要な理由を、この章で実際に使ったコマンドを挙げて説明せよ。</strong></p><p><strong>ブラウザを通さなければ、HTML の属性は何の効力も持たないから</strong>です。この章の実測は全部 <code>curl</code> でやっています。</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --data</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;code=IT-BAD&amp;name=x&amp;quantity=abc&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:18081/inventory/app/create</span></span></code></pre></div><p>このリクエストに <code>&lt;input type=&quot;number&quot;&gt;</code> は一切関係ありません。ブラウザ以外からのリクエストは、いくらでも作れます。</p><blockquote><p><strong>クライアント側の検証は「親切」であって「守り」ではありません。</strong></p></blockquote><p><code>type=&quot;number&quot;</code> は打ち間違えたその場で教えてくれるので<strong>入れる価値はあります</strong>が、それはサーバ側の検証の代わりにはなりません。この章の完了時点で <code>Tests run: 32</code> が緑です。</p><hr><h2 id="第10章" tabindex="-1">第10章 <a class="header-anchor" href="#第10章" aria-label="Permalink to &quot;第10章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/10-mail-xml">第10章 アプリの外へ — Jakarta Mail と JAXB</a></p></blockquote><p><strong>1. 「アプリの外に出る」ときに共通して現れる3つの困難を挙げよ。メールと XML で、それぞれどういう形で現れたか。</strong></p><blockquote><p><strong>アプリの外に出た瞬間、「相手がいる」という一点から、同じ3つの困難が同時に生まれる。</strong><strong>(a) 文字コードを明示しないと化ける (b) 相手が落ちていると自分も失敗する (c) テストで本物を使いにくい</strong></p></blockquote><p><strong>(a)</strong> メールでは <code>setSubject(..., &quot;UTF-8&quot;)</code> と <code>setText(..., &quot;UTF-8&quot;)</code> の第2引数として、XML では <code>Content-Type</code> の <code>charset=UTF-8</code> として現れました。<strong>String をバイト列にする瞬間、必ず選択が発生します。</strong> <strong>(b)</strong> メールでは MailHog が落ちていると <code>Transport.send()</code> が例外を投げ、在庫は更新済みなのに 500 が返ります（→ Q12）。<strong>(c)</strong> SMTP を叩くテストは <code>./mvnw test</code> を Docker 依存にしてしまうので <code>RecordingMailer</code> で差し替え、XML は往復テストで「情報が失われないこと」を見る形にしました。</p><p><strong>2. JAXB を使うのに依存が2つ要るのはなぜか。同じ構造を、2章と8章と9章のどこで見たか。</strong></p><p><strong>仕様（API）と実装が別の JAR に分かれているから</strong>です。<code>jakarta.xml.bind:jakarta.xml.bind-api</code>（4.0.5）が API、<code>org.glassfish.jaxb:jaxb-runtime</code>（4.0.9）が実装で、API だけ入れると実行時に実装が見つからず落ちます。この教材ではこれが<strong>4回目</strong>です。</p><table tabindex="0"><thead><tr><th>仕様（API）</th><th>実装</th><th>初出</th></tr></thead><tbody><tr><td><code>jakarta.servlet.*</code></td><td>Tomcat</td><td>序章</td></tr><tr><td><code>jakarta.tags.*</code>（JSTL）</td><td><code>org.glassfish.web:jakarta.servlet.jsp.jstl</code></td><td><strong>2章</strong></td></tr><tr><td><code>jakarta.persistence.*</code>（JPA）</td><td>Hibernate ORM</td><td><strong>8章</strong></td></tr><tr><td><code>jakarta.validation.*</code>（Bean Validation）</td><td>Hibernate Validator</td><td><strong>9章</strong></td></tr><tr><td><code>jakarta.xml.bind.*</code>（JAXB）</td><td><code>org.glassfish.jaxb:jaxb-runtime</code></td><td><strong>10章</strong></td></tr></tbody></table><p>2章の JSTL とは構造が完全に同じで、<strong><code>org.glassfish</code> という団体名まで一致しています</strong>。書く <code>import</code> はすべて <code>jakarta.xml.bind.*</code> で、<code>org.glassfish.jaxb</code> は <code>pom.xml</code> にしか出てきません。なお Jakarta Mail（<code>com.sun.mail:jakarta.mail</code> 2.0.2）は API と実装が1つの JAR に入っているので、例外的に1依存で済みます。</p><p><strong>3. <code>javax.sql.DataSource</code>（8章）は <code>javax</code> のままなのに、JAXB は <code>javax.xml.bind</code> から <code>jakarta.xml.bind</code> に変わった。この違いを判定する基準は何か。</strong></p><p>8章と同じ基準です。</p><blockquote><p><strong>その API は JDK に入っているか。入っているなら <code>javax</code>、別途 JAR を足すものなら <code>jakarta</code>。</strong></p></blockquote><p><code>DataSource</code> は JDBC の一部で、JDBC は Java SE なので JDK に入っており <code>javax</code> のままです。JAXB はこう推移しました。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>JDK 8    javax.xml.bind.* が JDK に入っている  → 何も足さずに使えた</span></span>\n<span class="line"><span>JDK 9    非推奨（--add-modules java.xml.bind で使えた）</span></span>\n<span class="line"><span>JDK 11   ★JDK から完全に削除</span></span>\n<span class="line"><span>その後   jakarta.xml.bind.* として独立ライブラリに</span></span></code></pre></div><p>つまり今の JAXB は<strong>別途 JAR を足すもの</strong>なので <code>jakarta</code> 側です。JDK 8 時代の記事を真似すると <code>シンボルを見つけられません シンボル: クラス XmlRootElement</code> になります（→ <a href="./../chapters/08-jpa">8章</a>）。</p><p><strong>4. メールの件名が <code>=?UTF-8?B?W+Wc...?=</code> という形になるのはなぜか。<code>setSubject</code> の第2引数を省くと何が起きるか。</strong></p><p><strong>メールのヘッダは ASCII しか運べないから</strong>です。SMTP とメッセージ形式の原型が決まったのは 1980 年代で、前提は 7 ビットの ASCII だけでした。だから日本語の件名は3段階で変換されます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>① UTF-8 のバイト列にする   ← ★どの文字コードにするか決める必要がある</span></span>\n<span class="line"><span>② base64 で ASCII 文字だけに変換する</span></span>\n<span class="line"><span>③ 「これは UTF-8 を base64 したものだ」と印を付ける</span></span></code></pre></div><p>この形式を <strong>MIME encoded-word</strong> と呼びます。<code>=?</code> が始まりの印、<code>UTF-8</code> が元の文字コード、<code>B</code> が符号化方式（B = base64）、その後ろが本体、<code>?=</code> が終わりの印です。実測の <code>=?UTF-8?B?W+WcqOW6q+WIh+OCjF0g44Oh44O844Or44OG44K544OI?=</code> をデコードすると <code>[在庫切れ] メールテスト</code> になります。</p><p><strong><code>setSubject</code> の第2引数は、この①で使う文字コード</strong>です。省くと Jakarta Mail は<strong>実行環境の既定の文字コード</strong>を使い、それが UTF-8 とは限りません（<strong>Windows なら Shift_JIS のことがあります</strong>）。受け取った側は <code>=?UTF-8?B?...?=</code> と書いてあるとおりに UTF-8 で解こうとするので、<strong>化けます。</strong> なお本文側は <code>Content-Transfer-Encoding: base64</code> と <code>Content-Type: text/plain; charset=UTF-8</code> という別の仕組みで、後者が <code>setText</code> の第2引数から来ています。</p><p><strong>5. この教材で、文字コードを明示した箇所を5つ挙げよ（1章・4章・9章・10章×2）。5つに共通する構造を1文で述べよ。</strong></p><table tabindex="0"><thead><tr><th>章</th><th>どこで</th><th>何を明示したか</th></tr></thead><tbody><tr><td>1章</td><td><code>response.setContentType(&quot;text/html; charset=UTF-8&quot;)</code></td><td>HTTP レスポンスの文字コード</td></tr><tr><td>4章</td><td><code>request.setCharacterEncoding(&quot;UTF-8&quot;)</code>（<code>EncodingFilter</code>）</td><td>HTTP リクエストの文字コード</td></tr><tr><td>9章</td><td><code>source.setDefaultEncoding(&quot;UTF-8&quot;)</code></td><td>プロパティファイルの文字コード</td></tr><tr><td>10章</td><td><code>setSubject(..., &quot;UTF-8&quot;)</code> / <code>setText(..., &quot;UTF-8&quot;)</code></td><td>メールの文字コード</td></tr><tr><td>10章</td><td><code>new MediaType(APPLICATION_XML, UTF_8)</code></td><td>XML レスポンスの文字コード</td></tr></tbody></table><p>共通する構造はこれです。</p><blockquote><p><strong>バイト列だけを渡しても、相手はそれをどう読めばいいか分からない。「このバイト列は UTF-8 だ」という宣言を、バイト列とは別に渡す必要がある。</strong></p></blockquote><p>そして5回とも「<strong>省くと動くが、日本語だけ壊れる</strong>」という壊れ方をします。英語だけでテストすると1つも気づけません——<code>&quot;OK&quot;</code> は UTF-8 でも Shift_JIS でも ISO-8859-1 でも同じ <code>4F 4B</code> ですが、<code>&quot;在庫&quot;</code> は UTF-8 が <code>E5 9C A8 E5 BA AB</code>、Shift_JIS が <code>8D DD 8C C9</code> と全く違い、ISO-8859-1 ではそもそも表現できないからです。</p><p><strong>6. <code>/export</code> の日本語が全部 <code>?</code> になったとき、XML 宣言には <code>encoding=&quot;UTF-8&quot;</code> と書いてあった。それでも壊れたのはなぜか。</strong></p><p><strong>XML 宣言はデータの中の自己申告でしかなく、実際にどのバイト列で書き出すかを決めているのは HTTP の <code>Content-Type</code> ヘッダだから</strong>です。このときヘッダは <code>Content-Type: application/xml</code> で <strong>charset が無く</strong>、Spring は「charset の指定がない <code>application/xml</code>」を書き出すとき既定で <strong>ISO-8859-1</strong> を使います。ISO-8859-1 は西欧の文字しか表現できないので、日本語はすべて <code>?</code>（<code>0x3F</code>）に潰されました。</p><blockquote><p><strong>外側の宣言が、内側の宣言に勝つ。</strong></p></blockquote><p>修正の前も後も、XML 宣言の1行目は同一（<code>Marshaller.JAXB_ENCODING</code> が書かせた行）です。直したのは <code>.contentType(new MediaType(MediaType.APPLICATION_XML, StandardCharsets.UTF_8))</code> の1箇所で、<code>Content-Type: application/xml;charset=UTF-8</code> になった途端に <code>&lt;name&gt;ボールペン（黒）&lt;/name&gt;</code> が出るようになりました。なお <code>produces</code> はメソッドが返せる形式の宣言（入口の条件）であって、<strong>実際に効くのは <code>contentType</code>（出口の宣言）のほう</strong>です。ファイルに保存して渡す場合は HTTP ヘッダが付かないので、逆に XML 宣言だけが手がかりになります——<code>JAXB_ENCODING</code> はそのときのために意味があります。</p><p><strong>7. 化けているのが「データの破壊」なのか「端末の表示の問題」なのかを、どうやって切り分けたか。実際に打ったコマンドを挙げよ。</strong></p><p><strong><code>od -c</code> でバイト列を直接見ました。</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:18081/inventory/app/export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;5p&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> od</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -c</span></span></code></pre></div><p>判定基準はこうです。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>もし表示の問題なら:  E3 83 9C E3 83 BC ...   （UTF-8 のバイト列がそのまま出ている）</span></span>\n<span class="line"><span>実際に出ていたのは:  3F 3F 3F 3F 3F 3F ...   ★全部 &#39;?&#39; に置き換わっている</span></span></code></pre></div><p>つまり<strong>データが壊れており、復元できません。</strong> 端末の設定をいくら直しても元の文字は戻りません。ヘッダは <code>curl -s -D - -o /dev/null ... | grep -i content-type</code> で確認しました。</p><p><strong>8. <code>@ResponseBody</code> を付けると、返り値の <code>String</code> の意味がどう変わるか。7章の <code>ViewResolver</code> は、このとき何をしているか。</strong></p><table tabindex="0"><thead><tr><th></th><th><code>@ResponseBody</code> なし</th><th><code>@ResponseBody</code> あり</th></tr></thead><tbody><tr><td>返り値の意味</td><td><strong>ビューの名前</strong></td><td><strong>レスポンスの本文そのもの</strong></td></tr><tr><td>JSP を通るか</td><td><strong>通る</strong></td><td><strong>通らない</strong></td></tr><tr><td>このアプリの例</td><td><code>/list</code> <code>/new</code> <code>/create</code></td><td><strong><code>/export</code></strong></td></tr></tbody></table><p><code>@ResponseBody</code> なしのとき、<code>ViewResolver</code> は prefix <code>/WEB-INF/views/</code> と suffix <code>.jsp</code> を付けて <code>&quot;item-list&quot;</code> を <code>/WEB-INF/views/item-list.jsp</code> に変換し、JSP を実行します。<code>@ResponseBody</code> を付けると<strong>この変換が起きず、ViewResolver を通りません</strong>。<strong>同じ <code>String</code> を返しているのに、意味が正反対になります。</strong> 付け忘れると <code>/WEB-INF/views/&lt;?xml version...&gt;.jsp</code> を探しに行って 500 になります。</p><p>XML を JSP で組み立てない理由もここにあります——エスケープを1箇所でも忘れると壊れた XML が出ますが、JAXB はその心配をしなくてよいように組み立ててくれます。<strong><code>@ResponseBody</code> は「文字列の組み立てはもう終わっている」という宣言</strong>です。</p><p><strong>9. <code>ItemXml</code> に <code>@XmlAccessorType(XmlAccessType.FIELD)</code> を付けている理由を述べよ。付けないと何が起きるか。9章の <code>ItemForm</code> とは、なぜ setter の要否が逆になるのか。</strong></p><p>理由は <strong>setter を書かずに済むから</strong>です。<code>FIELD</code> は「フィールドだけ（<code>private</code> も含む）を見る。getter は無視する」という指定なので、JAXB がリフレクションでフィールドに直接書き込みます。<code>ItemXml</code> には getter しかなく、setter は1つもありません。</p><p>付けないと既定は <code>PUBLIC_MEMBER</code>（<code>public</code> な getter/setter の組と <code>public</code> フィールド）になります。アノテーションが付いたフィールドは <code>private</code> でも対象に含まれるので、<code>private String code</code>（<code>@XmlElement</code> 付き）と <code>public String getCode()</code> の<strong>両方が「code」として登録され</strong>、<code>JAXBContext</code> を作る時点で落ちます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>IllegalAnnotationsException: Class has two properties of the same name &quot;code&quot;</span></span></code></pre></div><p>9章の <code>ItemForm</code> と setter の要否が逆になるのは、<strong>値を入れる主体と入れ方が違う</strong>からです。</p><table tabindex="0"><thead><tr><th></th><th><code>ItemForm</code>（9章）</th><th><code>ItemXml</code>（10章）</th></tr></thead><tbody><tr><td>誰が値を入れるか</td><td>Spring のデータバインダ</td><td>JAXB</td></tr><tr><td>入れ方</td><td><strong>setter を呼ぶ</strong>（JavaBeans 規約）</td><td><strong>フィールドに直接書く</strong>（<code>FIELD</code>）</td></tr><tr><td>setter</td><td><strong>必要</strong></td><td><strong>不要</strong></td></tr></tbody></table><p>おかげで「<strong>setter を書き忘れると値が入らない</strong>」という9章の落とし穴が、<code>FIELD</code> を指定した時点で構造的に消えています。</p><p><strong>10. <code>ItemXml</code> に引数なしコンストラクタが要る理由を、8章の <code>Item</code> と対比して述べよ。なぜ「引数ありのコンストラクタ」ではダメなのか。</strong></p><p>8章の <code>Item</code> とまったく同じ理由です。**「空の箱を作ってから、名前を頼りにフィールドを埋める」**という手順だからです。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>① XML から &lt;item&gt; を1つ読む</span></span>\n<span class="line"><span>② new ItemXml()  ← ★引数なしコンストラクタ（code=null, name=null, quantity=0 の空箱）</span></span>\n<span class="line"><span>③ フィールドに値を入れる（リフレクション）</span></span></code></pre></div><p>この要求は3章分で共通しています——<strong>8章は JPA が DB の1行から、9章は Spring のデータバインダが HTTP パラメータから、10章は JAXB が XML の要素から</strong>、それぞれオブジェクトを作ります。</p><p>引数ありではダメなのは、<strong>ライブラリがコンストラクタの引数の順番を知らないから</strong>です。XML にあるのは <code>&lt;code&gt;</code> <code>&lt;name&gt;</code> <code>&lt;quantity&gt;</code> という<strong>名前</strong>だけで、順番は保証されませんし、要素が欠けているかもしれません。引数の名前もコンパイル後に消えることがあります（<code>-parameters</code> なしだと <code>arg0, arg1…</code>）。引数なしで作ってから埋めるなら、名前が一致するフィールドに入れるだけで済み、欠けている要素はその項目が初期値のまま残ります。代償は8章と同じく <strong><code>final</code> を諦めること</strong>です。</p><p><strong>11. <code>JAXBContext</code> はフィールドに持ち、<code>Marshaller</code> は毎回作る。この非対称の理由を2つ述べよ。3章・4章のどの話と繋がるか。</strong></p><p><strong>(1) コストが桁違いだから。</strong> <code>JAXBContext.newInstance(...)</code> は、<code>ItemListXml</code> と <code>ItemXml</code> のアノテーションを全部読み、フィールドの型を調べ、XML との対応表を組み立てます——<strong>重く、しかも結果は毎回同じ</strong>です。<code>context.createMarshaller()</code> は、組み上がった対応表を使う「係」を1人作るだけなので軽い。<strong>(2) スレッド安全性が違うから。</strong></p><blockquote><p><strong><code>JAXBContext</code> はスレッドセーフ。<code>Marshaller</code> / <code>Unmarshaller</code> はスレッドセーフではない。</strong></p></blockquote><p>繋がるのは、**3章の「Servlet のインスタンスは1個、複数スレッドが同時に呼ぶ」**と、**4章の「Filter に書き換わる状態を持たせてはいけない」**です。Bean も1個なので、<code>Marshaller</code> をフィールドに持つと <strong>A の出力の途中に B の出力が混ざります</strong>。「Bean は1個」という事実は6章から一貫して効き続けています。</p><p>ついでに用語を確認しておくと、**marshal がオブジェクト → XML（詰める、<code>export()</code>）、unmarshal が XML → オブジェクト（解く、<code>importFrom()</code>）**です。<code>un-</code> が付いているほうが「読み込み」です。</p><p><strong>12. 在庫の更新は成功したのにメール送信が失敗したとき、どうすべきか。この教材が選んだ案と、その弱点を述べよ。他の3案は何か。</strong></p><table tabindex="0"><thead><tr><th>案</th><th>やること</th><th>良い点</th><th>悪い点</th></tr></thead><tbody><tr><td><strong>A（この教材）</strong></td><td>例外を投げる</td><td>失敗が見逃されない</td><td><strong>在庫の成功が 500 に見える</strong></td></tr><tr><td>B</td><td>ログに書いて握りつぶす</td><td>在庫の更新は成功として返る</td><td><strong>1通も届いていなくても誰も気づかない</strong></td></tr><tr><td>C</td><td>在庫の更新も巻き戻す</td><td>状態が一致する</td><td><strong>メールサーバが落ちただけで在庫が更新できない</strong></td></tr><tr><td>D</td><td>送信要求を DB のテーブルに1行書き、別プロセスが後で送る</td><td>上の全部を解決する</td><td><strong>仕組みが増える</strong></td></tr></tbody></table><p><strong>この教材が選んだのは A</strong> で、理由は「<strong>失敗が黙って消えるのが一番怖い</strong>」から、そして学習用アプリなので<strong>間違いが目に見えるほうがよい</strong>という判断です。<strong>弱点</strong>は、在庫の更新自体はすでに別トランザクションでコミット済みなのに 500 が返ることで、ユーザーの認識（失敗した）と DB の実態（成功している）がズレます。ユーザーがもう一度押すと在庫が -1 になろうとします（6章のルールで拒否されるので事故にはなりませんが、認識のズレ自体が問題です）。</p><p>**A は正解ではありません。実務の定石は D（アウトボックスパターン）**です。C は明らかに行き過ぎで、「メールが送れないから在庫を動かせない」では業務が止まります。この難しさの核心はここにあります。</p><blockquote><p><strong>外部への通知は、本来の業務より重要度が低い。にもかかわらず、外部への通知は本来の業務より失敗しやすい。この非対称性が、外部連携の設計を難しくしている。</strong></p></blockquote><p><strong>13. <code>StockAlertTest</code> の3件目（在庫 0 に -1）が守っているルールは何か。これが無いと、何回ボタンを押すと何通メールが飛ぶか。</strong></p><p>守っているのは <strong>「拒否された操作では送らない」</strong>、コードでいえば <code>if (!changed) { return false; }</code> です。3件のテストは境界を挟んで並んでいます。</p><table tabindex="0"><thead><tr><th>テスト</th><th>在庫</th><th>操作</th><th>結果</th><th>守っているルール</th></tr></thead><tbody><tr><td>1件目</td><td>1</td><td>-1</td><td><strong>送る</strong></td><td>0 に<strong>なった</strong>ら送る</td></tr><tr><td>2件目</td><td>5</td><td>-1</td><td>送らない</td><td>4 なので、まだ 0 ではない</td></tr><tr><td>3件目</td><td><strong>0</strong></td><td>-1</td><td>送らない</td><td><strong>拒否された操作では送らない</strong></td></tr></tbody></table><p><strong>「在庫が 0 である」と「在庫が 0 になった」は違います。通知したいのは後者です。</strong> このルールが無いと、<strong>在庫 0 の商品の <code>-1</code> ボタンを押すたびにメールが飛びます——10回押したら10通届きます。</strong> しかも1件目も2件目も緑のままなので、境界のテストが無ければ気づけません。</p><blockquote><p><strong>境界のテストは、正常系より先に書く価値があります。</strong> 正常系は手で動かせば分かりますが、<strong>境界は手で試そうと思いつかない</strong>からです。</p></blockquote><p><strong>14. 往復テスト（round trip）が、<code>export</code> だけのテストより強力なのはなぜか。テストデータが <code>&quot;往復テスト（日本語）&quot;</code> である理由を述べよ。</strong></p><p><strong>「XML の形」ではなく「情報が失われないこと」を検証しているから</strong>です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>★export だけのテスト</span></span>\n<span class="line"><span>   &lt;name&gt;ボールペン&lt;/name&gt; が含まれるか、と書いてある</span></span>\n<span class="line"><span>   → ★「期待する XML の形」をテストに書き写している</span></span>\n<span class="line"><span>   → 要素名を &lt;itemName&gt; に変えたら、テストも書き換えることになる</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>★往復テスト</span></span>\n<span class="line"><span>   ★XML の中身を1文字も書いていない</span></span>\n<span class="line"><span>   → 要素名を &lt;itemName&gt; に変えても、往復するなら緑のまま</span></span></code></pre></div><p>しかも片方向のテストでは検出できない壊れ方を拾えます——<strong><code>export</code> が書く要素名と <code>import</code> が読む名前が違う</strong>（両方が同じ名前で書いてあれば片方向では気づけない）、<strong>型変換で桁が落ちる</strong>、<strong>文字コードのどこかで日本語が壊れる</strong>。</p><p>テストデータが <code>&quot;往復テスト（日本語）&quot;</code> なのは、<strong>ASCII だけのデータで往復テストを書くと、文字コードの事故を1つも検出できないから</strong>です。<code>（</code> という全角の括弧まで入っているのは、「よくある半角/全角の取り違え」も同時に踏むためです。</p><blockquote><p><strong>外部連携のテストは、往復させる。そしてデータには必ず ASCII の外の文字を入れる。</strong></p></blockquote><p>この形は XML に限らず、JSON でも CSV でも、シリアライズを伴う連携ならすべて同じ形でテストできます。</p><p><strong>15. <code>RecordingMailer</code> が <code>StockAlertMailer</code> を継承しているのは、6章の <code>FakeItemRepository</code> と比べて何が後退しているか。それでもそうした理由は何か。</strong></p><p>6章では <code>ItemRepository</code>（インターフェース）の下に本物（<code>JpaItemRepository</code>）と偽物（<code>FakeItemRepository</code>）が<strong>対等に並び、互いを知りませんでした</strong>。10章の <code>RecordingMailer</code> は本物のクラスを <code>extends</code> しているので、<strong>偽物が本物に依存しています</strong>。</p><table tabindex="0"><thead><tr><th>問題</th><th>何が起きるか</th></tr></thead><tbody><tr><td>本物のコンストラクタが必ず走る</td><td><code>super(...)</code> を呼ばざるを得ない</td></tr><tr><td><code>notifyOutOfStock</code> が <code>final</code> になったら壊れる</td><td>上書きできなくなる</td></tr><tr><td>メソッドが増えたら、上書き漏れに気づけない</td><td>偽物が本物の挙動を継いでしまう</td></tr></tbody></table><p><strong>それでも継承にしたのは、クラスをこれ以上増やしたくないから</strong>です。インターフェースに切ると <code>StockAlertMailer</code>（interface）・<code>SmtpStockAlertMailer</code>（本物）・<code>RecordingMailer</code>（偽物）で<strong>3つ</strong>になります。</p><blockquote><p><strong>外部と繋がる部品は、テストで差し替えたくなる。差し替えたくなるものは、インターフェースに切っておく。</strong></p></blockquote><p><strong>この教材は、その原則を10章で1つ破っています。破ったことを書き残してあります</strong>（演習4でインターフェースに直すところまでやります）。なお <code>super(&quot;localhost&quot;, 0)</code> と<strong>明らかに使われない値</strong>を渡しているのは、引数なしコンストラクタだと <code>System.getenv()</code> を読んで <code>host = &quot;mail&quot;</code> を設定してしまうからで、「このオブジェクトは接続しない」ことをコードに書くための書き方です。</p><p><strong>16. <code>ItemService</code> に「引数1つのコンストラクタ」を残さなかったのはなぜか。残すと、既存のテストで何が起きるか。</strong></p><p>残すと、<strong>既存のテストが「本物の Mailer」を使うようになるから</strong>です。テストは緑のままコンパイルが通り、<strong>実行時に SMTP へ接続しに行って落ちます</strong>（あるいは、落ちずに本当にメールを送ってしまいます）。</p><p>残さなかったので、代わりにコンパイルエラーが出ました。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>ItemServiceTest.java:23: エラー: クラス ItemService のコンストラクタ ItemService は</span></span>\n<span class="line"><span>指定された型に適用できません。</span></span>\n<span class="line"><span>  期待値: ItemRepository,StockAlertMailer</span></span>\n<span class="line"><span>  検出値: ItemRepository</span></span></code></pre></div><p><strong>「コンパイルエラーを消すために、危険な既定値を用意する」は、よくある事故です。面倒でも、全部の呼び出し側を明示的に直します。</strong></p><blockquote><p><strong>「変更したときに、影響範囲が自動で列挙されること」は、静的型付け言語の最大の利点の1つです。</strong></p></blockquote><p>9章では「setter を書き忘れるとコンパイルは通り、実行時例外も出ない」という逆の状況を見ました。ここではその利点が働いています。この章の完了時点で <code>Tests run: 40</code> が緑です（9章の32件に <code>StockAlertTest</code> 3件と <code>ItemXmlPortTest</code> 5件を足したもの）。ちなみに <code>StockAlertTest</code> は <strong>0.005 秒</strong>——<strong>メール送信のテストが、DB のテスト（<code>JpaItemRepositoryTest</code> 2.322 秒）の 460 分の1の時間で終わっています。</strong></p><blockquote><p><strong>テストの実行時間は、「そのテストが何に触っているか」の告発です。</strong></p></blockquote><hr><h2 id="第11章" tabindex="-1">第11章 <a class="header-anchor" href="#第11章" aria-label="Permalink to &quot;第11章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/11-batch">第11章 夜にまとめて処理する — Spring Batch</a></p></blockquote><p><strong>1. <code>for</code> 文で全件を回す実装が、100万件で壊れる理由を4つ挙げよ。そのうち「Spring Batch を使う最大の理由」はどれか。</strong></p><p><strong>(1) メモリが持たない</strong>——<code>Item</code> 1件を仮に 200 バイトとすると、100万件で <strong>200 MB</strong>、1000万件で <strong>2 GB</strong>。Tomcat のヒープが 512 MB なら 100万件で危険域、1000万件で確実に <code>OutOfMemoryError</code> です。しかも永続化コンテキストが読んだエンティティを全部抱え込むので、実際の消費はこの倍以上になります。<strong>(2) トランザクションが1本だと、ロックも1本</strong>——100万件に 30 分かかるなら、トランザクションが 30 分開きっぱなしで、同じ行を触る他の処理が待たされ、PostgreSQL は古い行のバージョンを保持し続けてディスクも食います。<strong>(3) 途中で失敗したら、最初からやり直すしかない</strong>——60万件目で例外が出ると <code>ROLLBACK</code> で 59万9999件が無かったことになり、翌朝また 30 分。しかも「どこまで終わったか」の記録がないので「本当に全部やり直して大丈夫か（二重に処理されないか）」も分かりません。<strong>(4) 誰も「昨日の分は終わった」を知らない</strong>——正常終了したのか、何件処理したのか、いつ始まっていつ終わったのか、二重起動したらどうなるか、3日前の分だけ流し直せるか——<strong>全部分かりません</strong>。</p><p><strong>最大の理由は (3)</strong> です。</p><blockquote><p><strong>バッチが難しいのは「大量に処理すること」ではない。「途中で失敗したときに、どこから再開するか」を決めることである。</strong></p></blockquote><p><strong>2. chunk（区切ってコミットする）は、4つの破綻のうちどれを解いているか。<code>JobRepository</code>（実行履歴を DB に持つ）はどれを解いているか。</strong></p><table tabindex="0"><thead><tr><th>破綻</th><th>必要なもの</th></tr></thead><tbody><tr><td>(1) メモリが持たない</td><td>区切って読む</td></tr><tr><td>(2) ロックが長い</td><td><strong>区切ってコミットする</strong>（= chunk）</td></tr><tr><td>(3) やり直せない</td><td><strong>区切ってコミットする</strong> + <strong>どこまで終わったかの記録</strong></td></tr><tr><td>(4) 二重起動が防げない</td><td><strong>実行履歴の記録</strong>（= JobRepository）</td></tr></tbody></table><p><strong>chunk が解くのは (2) と、(3) の「区切る」部分</strong>です。「区切ってコミットする」だけで、失敗の被害が 100万件から 1000件に減ります。<strong><code>JobRepository</code> が解くのは (3) の「記録」部分と (4)</strong> です。Spring Batch はこの2つを枠組みとして提供する道具で、<strong>それ以外の機能（並列実行・リトライ・スキップ・リスタート）は全部この2つの上に乗っています。</strong></p><p><strong>3. Job と Step を分ける意味を「再開」という言葉を使って説明せよ。</strong></p><blockquote><p><strong>Step の切れ目 = 再開できる地点。</strong></p></blockquote><p>だから「Step をいくつに分けるか」は「<strong>どこから再開したいか</strong>」で決めます。たとえば Step 1「在庫 0 の商品を書き出す」に 30 分かかっていて、Step 3「担当者にメールを送る」だけが失敗したとき、Step で分かれていれば <strong>Step 3 から再開できます</strong>。1つの Step に全部書いていたら、30 分の Step 1 からやり直しです。この章では Step は1つにしています——概念モデルを掴むのが目的で、2つ目を足しても学べることが増えないからです。</p><p><strong>4. <code>ItemReader</code> と <code>ItemProcessor</code> は1件ずつなのに、<code>ItemWriter</code> だけがまとめて受け取るのはなぜか。もし Writer も1件ずつだったら何ができなくなるか。</strong></p><p><strong>DB への書き込みは、まとめたほうが圧倒的に速いから</strong>です。設計ミスではなく意図的です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1件ずつ書く場合: INSERT ...;（往復1）INSERT ...;（往復2）… 1000件なら往復1000回</span></span>\n<span class="line"><span>まとめて書く場合: INSERT ..., ..., ...;（往復1）  ← ★1000件を1回で</span></span></code></pre></div><p>DB との通信はネットワーク越しなので、<strong>往復の回数がそのまま時間になります</strong>。1000 回が 1 回になれば桁で速くなります。もし <code>ItemWriter.write(String line)</code> という1件ずつの API だったら、<strong>まとめて書くことが原理的にできません。</strong></p><blockquote><p><strong>Reader は「取り出しの単位」に合わせて1件ずつ。Writer は「書き込みの効率」に合わせてまとめて。</strong> 同じ Step の中で単位が違うのは、最適化したい対象が違うからです。</p></blockquote><p>3役に分けてあるのは<strong>差し替えられるようにするため</strong>で、6章の <code>ItemRepository</code> インターフェースと同じ構図です——読み元を DB から CSV に変えたいなら Reader だけ、出力先を変えたいなら Writer だけ、集計のルールを変えたいなら Processor だけを差し替えます。</p><p><strong>5. <code>chunk(2)</code> の <code>2</code> は何の数か。この値を大きくしたときの利点と欠点を2つずつ挙げよ。</strong></p><p>**「何件ごとに Writer を呼んでコミットするか」**の数です。</p><table tabindex="0"><thead><tr><th>chunk を大きくすると</th><th>chunk を小さくすると</th></tr></thead><tbody><tr><td>DB との往復が減って<strong>速い</strong></td><td>往復が増えて<strong>遅い</strong></td></tr><tr><td>失敗時に<strong>巻き戻る量が多い</strong></td><td>巻き戻る量が<strong>少ない</strong></td></tr><tr><td>メモリに<strong>多く抱える</strong></td><td>メモリが<strong>軽い</strong></td></tr><tr><td>ロックを<strong>長く握る</strong></td><td>ロックが<strong>短い</strong></td></tr></tbody></table><p><strong>利点は「速い」「DB との往復が減る」、欠点は「失敗時に巻き戻る量が多い」「メモリを多く抱える（＋ロックを長く握る）」<strong>です。要するに</strong>「速さ」と「失敗したときの被害」のトレードオフ</strong>で、実務では 100〜1000 あたりから始めます。この章が <code>chunk(2)</code> にしているのは<strong>動きを目で追えるようにするため</strong>です——5件のデータで <code>chunk(1000)</code> にしたら、コミットは1回しか起きず観察できません。</p><p><strong>6. <code>chunk(2)</code> で5件を処理すると、Writer は何回呼ばれるか。そう考えた根拠を述べよ。</strong></p><p><strong>3回</strong>です。根拠は「<code>chunk(2)</code> は『必ず2件』ではなく『<strong>最大2件</strong>』で、最後の端数はそのまま書き出される」という性質です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>read(1) → process(1) ┐</span></span>\n<span class="line"><span>read(2) → process(2) ┤ → write([1, 2]) → COMMIT ①</span></span>\n<span class="line"><span>read(3) → process(3) ┐</span></span>\n<span class="line"><span>read(4) → process(4) ┤ → write([3, 4]) → COMMIT ②</span></span>\n<span class="line"><span>read(5) → process(5) ┐</span></span>\n<span class="line"><span>read() → null         ┤ → write([5])    → COMMIT ③</span></span></code></pre></div><p>2 + 2 + 1 で3回、式にすれば <code>ceil(5 / 2.0) = 3</code> です。ステップ7で実測して確かめました（<code>[PROBE] before=2 read=5 write=5 commit=3</code>）——<strong>空のコミットは足されません</strong>（→ Q15）。</p><p><strong>7. <code>@StepScope</code> を付けないと、なぜ「0件で成功」になるのか。Bean がいつ作られるかという観点で説明せよ。</strong></p><p><strong><code>@Bean</code> メソッドは、コンテナの起動時に1回だけ呼ばれ、戻り値がシングルトンとして固定されるから</strong>です（6章で自作した DI コンテナと同じです）。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>時刻 T0: Spring コンテナ起動 → outOfStockReader(repository) が呼ばれる</span></span>\n<span class="line"><span>         repository.findAll() → ★まだ何も入っていない → []</span></span>\n<span class="line"><span>         ★空の List を握った Reader が、Bean として固定される</span></span>\n<span class="line"><span>時刻 T1: テストが商品を3件足す → H2 には確かに入っている</span></span>\n<span class="line"><span>         → ★しかし Reader が握っている List は空のまま</span></span>\n<span class="line"><span>時刻 T2: ジョブ起動 → read() → null → 1件も読まずに Step 終了 → ★COMPLETED（成功扱い）</span></span></code></pre></div><p>つまり Reader は**「起動した瞬間の在庫」を握ったまま固まっていました**。<code>@StepScope</code> は「<strong>この Bean は、Step が始まるたびに作り直す</strong>」という指定なので、付けると Step 開始のたびに <code>repository.findAll()</code> が走り、<strong>ジョブを流したその時点の在庫</strong>を読みます。</p><blockquote><p><strong><code>@Bean</code> メソッドの中で <code>findAll()</code> のような「今の状態を読む処理」を書いたら、スコープを疑う。その <code>@Bean</code> は、あなたが思っているより1回しか呼ばれていません。</strong></p></blockquote><p><strong>8. その不具合が「テストの書き方の問題」ではなく「本番でも起きる」と言えるのはなぜか。本番ではどういう時系列で壊れるか。</strong></p><p><strong>本番でも「Bean が起動時に1回だけ作られる」という事実は同じだから</strong>です。本番ではこう壊れます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>09:00   Tomcat 起動 → outOfStockReader が作られる</span></span>\n<span class="line"><span>          → ★朝9時の在庫（0 の商品は 3 件）を握る</span></span>\n<span class="line"><span>09:00〜 一日中、商品が売れて在庫が減る</span></span>\n<span class="line"><span>          → 0 の商品が 3 件 → 20 件 → 47 件 に増える</span></span>\n<span class="line"><span>02:00   日次締めジョブが起動</span></span>\n<span class="line"><span>          → Reader が返すのは… ★朝9時の 3 件</span></span>\n<span class="line"><span>        「3 件を書き出した」で COMPLETED</span></span>\n<span class="line"><span>★44 件が締め漏れした。エラーは1つも出ていない</span></span></code></pre></div><p>しかも壊れ方が最悪です——<strong>例外は出ない、ステータスは <code>COMPLETED</code>、ログにエラーも出ない、監視でアラートも鳴らない。気づくのは月末に数字が合わなくて人間が見つけたとき</strong>です。「静かに間違った結果を出す」タイプで、落ちてくれるバグよりはるかに厄介です。<strong>テストでは気づきにくいのに、本番の夜中に静かに壊れる</strong>——この教材でもっとも実務的なタイプの落とし穴です。</p><p><strong>9. <code>@StepScope</code> の本来の用途（<code>JobParameters</code> を受け取る）と、この章で踏んだ症状は、どう繋がっているか。</strong></p><p><strong>同じ性質の表と裏</strong>です。本来の用途はこう書くためのものです。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Bean</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">StepScope</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ItemReader</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Item</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> reader</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(@</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Value</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;#{jobParameters[&#39;date&#39;]}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) String date) {</span></span></code></pre></div><p>「2026-08-16 分を締める」という引数は<strong>ジョブを起動するときに決まる</strong>もので、コンテナの起動時には存在しません。<strong>だから、Step が始まってから作る必要があります。</strong>「ジョブの実行ごとに変わるものを受け取れるようにする」——これが <code>@StepScope</code> が用意されている本当の理由です。</p><p><strong>この章で踏んだ「起動時の在庫を握って固まる」は、その裏返しの症状</strong>です。「起動時に作られると、実行時の情報が入らない」という同じ事実を、片方は機能として、片方は不具合として見ています。</p><p><strong>10. Spring Batch が DB にテーブル（<code>BATCH_JOB_INSTANCE</code> ほか）を要求するのはなぜか。アプリのメモリに記録するのでは、なぜだめか。</strong></p><p><strong>バッチを動かすプロセスは、途中で死ぬ前提だから</strong>です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>┌ アプリのメモリに記録する場合</span></span>\n<span class="line"><span>│   00:00 ジョブ開始、59万件処理</span></span>\n<span class="line"><span>│   00:18 ★JVM ごと落ちた（OOM / サーバ再起動）</span></span>\n<span class="line"><span>│   → 「59万件終わっていた」という記憶も一緒に消える → 再開できない</span></span>\n<span class="line"><span>┌ DB のテーブルに記録する場合</span></span>\n<span class="line"><span>│   00:00 BATCH_JOB_EXECUTION に「開始した」を INSERT</span></span>\n<span class="line"><span>│   00:05 BATCH_STEP_EXECUTION の read_count を更新</span></span>\n<span class="line"><span>│   00:18 ★JVM ごと落ちた</span></span>\n<span class="line"><span>│   → ★記録は DB に残っている → 「59万件目まで終わっていた」が分かる</span></span></code></pre></div><blockquote><p><strong>記録を、死ぬプロセスの外に置く。</strong> それが「DB のテーブルに書く」ということの意味です。</p></blockquote><p>なお、これらのテーブルは <code>@Entity</code> ではないので <strong>Hibernate の <code>hbm2ddl.auto</code> では作られません</strong>。テストでは <code>TestJpaConfig.dataSource()</code> に <code>.addScript(&quot;classpath:org/springframework/batch/core/schema-h2.sql&quot;)</code> を足して自分で流します。忘れると <code>テーブル &quot;BATCH_JOB_INSTANCE&quot; が見つかりません</code> で落ちます。スクリプトは <code>spring-batch-core</code> の JAR に DB ごとに1本ずつ同梱されています（本番の PostgreSQL なら <code>schema-postgresql.sql</code>）。</p><p><strong>11. <code>BATCH_JOB_INSTANCE</code> と <code>BATCH_JOB_EXECUTION</code> の違いを、「1回失敗して2回目に成功した日次バッチ」を例に説明せよ。</strong></p><table tabindex="0"><thead><tr><th></th><th>意味</th><th>例</th></tr></thead><tbody><tr><td><strong>Job Instance</strong></td><td>「<strong>どの仕事</strong>」</td><td>2026-08-16 分の日次締め</td></tr><tr><td><strong>Job Execution</strong></td><td>「<strong>その仕事を流した1回</strong>」</td><td>1回目（失敗）／2回目（成功）</td></tr></tbody></table><p>「1回失敗して2回目に成功した日次バッチ」では、<strong><code>BATCH_JOB_INSTANCE</code> は1行</strong>（<code>dailyClosingJob</code> × <code>date=2026-08-16</code>）ですが、<strong><code>BATCH_JOB_EXECUTION</code> は2行</strong>（1回目 <code>FAILED</code>、2回目 <code>COMPLETED</code>）になります。1つの Instance に何回でも Execution がぶら下がります。</p><p><strong>この2階層があるから、「昨日の分をもう一度流す」と「別の日の分を流す」を区別できます。</strong> さらに <code>BATCH_STEP_EXECUTION</code> が Step ごとの <code>READ_COUNT</code> / <code>WRITE_COUNT</code> / <code>COMMIT_COUNT</code> を持ちます。</p><p><strong>12. 同じ <code>JobParameters</code> で2回目を起動すると実行されないのは、どの破綻への答えか。日次バッチで日付を引数に渡すのはなぜか。</strong></p><p><strong>破綻(4)（二重起動を防げない）への答え</strong>です。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>02:00  cron が dailyClosingJob(date=2026-08-16) を起動 → COMPLETED</span></span>\n<span class="line"><span>02:05  担当者が「動いたか不安だから」と手で同じジョブを起動</span></span>\n<span class="line"><span>       BATCH_JOB_INSTANCE を見る → 既に COMPLETED</span></span>\n<span class="line"><span>       ★実行せずに JobInstanceAlreadyCompleteException</span></span></code></pre></div><p><strong>締め処理が二重に走って集計が倍になる事故が、枠組みのレベルで防がれています。</strong></p><p>日付を渡すのは、「1日1回だけ実行する」を成立させるためです。</p><table tabindex="0"><thead><tr><th>渡す引数</th><th>起こること</th></tr></thead><tbody><tr><td>引数なし</td><td><strong>2回目以降が永久に実行できない</strong>（同じ Instance と判定される）</td></tr><tr><td><code>date=2026-08-16</code></td><td>その日は1回だけ。翌日は別 Instance なので走る</td></tr><tr><td><code>time=(現在時刻)</code></td><td><strong>毎回別 Instance になり、二重起動が防げない</strong>（この保護を捨てる書き方）</td></tr></tbody></table><p>だから日次バッチには<strong>日付を <code>JobParameters</code> に渡すのが定石</strong>です。なおテストで落ちないのは、<code>JobLauncherTestUtils</code> が呼ばれるたびに一意な <code>JobParameters</code> を自動で付けているからで、<strong>テストの都合であって本番の挙動ではありません</strong>。</p><p><strong>13. <code>JobLauncherTestUtils</code> に set しなければならない3つを挙げ、それぞれの役割を述べよ。</strong></p><table tabindex="0"><thead><tr><th>set するもの</th><th>役割</th></tr></thead><tbody><tr><td><code>setJob(...)</code></td><td><strong>何を</strong>動かすか</td></tr><tr><td><code>setJobLauncher(...)</code></td><td><strong>誰が</strong>動かすか</td></tr><tr><td><code>setJobRepository(...)</code></td><td><strong>記録をどこに</strong>書くか</td></tr></tbody></table><p><strong>「動かすもの」だけでは足りず、「動かす人」と「記録係」も要る</strong>のです。忘れると、こうなります。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>NullPointerException: Cannot invoke &quot;...JobLauncher.run(...)&quot; because the return value of</span></span>\n<span class="line"><span>&quot;...JobLauncherTestUtils.getJobLauncher()&quot; is null</span></span></code></pre></div><p><code>jobLauncher</code> と <code>jobRepository</code> は <code>@EnableBatchProcessing</code> が登録済みなので、引数に書くだけで注入されます。ちなみに Boot には <code>@SpringBatchTest</code> があり、これを付けると3つ全部 set された状態で自動的に用意されます。</p><blockquote><p><strong>Boot を使っていたら、あなたはこのエラーを踏みません。同時に、「<code>JobLauncherTestUtils</code> には Job・JobLauncher・JobRepository の3つが要る」という事実も、一生知りません。</strong></p></blockquote><p><strong>14. このテストクラスに <code>@Transactional</code> を付けられないのはなぜか。8章の <code>JpaItemRepositoryTest</code> には付いているのに、何が違うのか。</strong></p><p><strong>バッチが自分でトランザクションを管理するから</strong>です。<code>.&lt;Item, String&gt;chunk(2, transactionManager)</code> の <code>transactionManager</code> が「Step が自分で BEGIN / COMMIT する」ことを意味しています。テスト側で外側のトランザクションを張ってロールバックしてしまうと、<strong>「chunk ごとにコミットする」という、この章で確かめたい振る舞いそのものが壊れます。</strong></p><blockquote><p><strong>テストを独立させる手段（ロールバック）と、検証したい対象（コミットの区切り）が衝突しました。こういうときは、検証したい対象を優先します。</strong></p></blockquote><p>テストの独立性は <code>before</code> を数えることで回復できますが、ロールバックしてしまったら「chunk ごとにコミットする」は永久に観察できないからです。</p><p>8章との違いは、<strong>何を検証したいか</strong>です。8章の <code>JpaItemRepositoryTest</code> は「1つのトランザクションの中での永続化コンテキストの振る舞い」を見ているので <code>@Transactional</code> が要ります。<strong>何を検証したいかで、付けるかどうかが決まります。</strong></p><p><strong>15. <code>commitCount</code> の期待値を、推測ではなく実測してから式にしたのはなぜか。最初の推測（<code>+ 1</code>）は、何を根拠にしていたか。</strong></p><p>最初の推測の根拠はこうでした。</p><blockquote><p>「Reader が <code>null</code> を返して『もう無い』と分かるためには、最後にもう一度 <code>read()</code> が呼ばれるはずだ。そのとき空の chunk ができて、<strong>空のコミットが1回足される</strong>のではないか」</p></blockquote><p>もっともらしい推測で、式にすれば <code>ceil(n / 2.0) + 1</code> になります。<strong>書かずに、測りました。</strong> <code>System.out.println</code> を1行仕込んで得た実測は <code>[PROBE] before=2 read=5 write=5 commit=3</code>——<strong><code>ceil(5 / 2.0) = 3</code>。空のコミットは足されませんでした。</strong></p><p>もし推測のまま <code>+ 1</code> を書いていたら、テストは赤くなり、「Spring Batch がおかしい」と疑って時間を溶かしていたはずです。</p><blockquote><p><strong>フレームワークの内部の挙動は、推測せずに測る。<code>System.out.println</code> を1行仕込むほうが、ドキュメントを30分読むより速いことがあります。</strong></p></blockquote><p>これは5章の「テストは実装の観察装置でもある」の続きで、<strong>テストを書く過程でフレームワークの振る舞いを1つ確定させました</strong>。最終的な式は <code>(long) Math.ceil((before + 3) / 2.0)</code> です。</p><p><strong>16. この章の <code>ListItemReader</code> は、4つの破綻のうちどれを解いていないか。実務ではどの Reader に差し替えるか。そのとき Processor / Writer / Step / Job は変わるか。</strong></p><p>解いていないのは **破綻(1)（メモリが持たない）**です。<code>findAll()</code> で全件を <code>List</code> に載せてから1件ずつ返しているだけだからです。この章の目的が「<strong>Job / Step / Reader-Processor-Writer という形を掴むこと</strong>」なので、本物の Reader を使うとページング・カーソル・フェッチサイズの話が一気に入ってきて肝心の形が見えなくなる、という判断です。</p><p>実務では読み元に応じて差し替えます。</p><table tabindex="0"><thead><tr><th>Reader</th><th>読み元</th><th>どう区切るか</th></tr></thead><tbody><tr><td><code>JpaPagingItemReader</code></td><td>DB（JPA）</td><td><code>LIMIT</code> / <code>OFFSET</code> でページごとに読む</td></tr><tr><td><code>JdbcCursorItemReader</code></td><td>DB（JDBC）</td><td>カーソルで1件ずつ引っ張る</td></tr><tr><td><code>FlatFileItemReader</code></td><td>CSV / 固定長ファイル</td><td>1行ずつ読む</td></tr><tr><td><code>StaxEventItemReader</code></td><td>XML</td><td>10章の JAXB と同じ領域。ただしストリームで読む</td></tr><tr><td><code>ListItemReader</code></td><td>メモリ上の <code>List</code></td><td><strong>区切らない</strong>（★この章）</td></tr></tbody></table><p>上の4つに共通するのは「<strong>全件をメモリに載せない</strong>」ことで、これが破綻(1)の本当の答えです。そして——</p><blockquote><p><strong><code>ListItemReader</code> を <code>JpaPagingItemReader</code> に差し替えるとき、Processor も Writer も Step も Job も、1行も変えません。</strong></p></blockquote><p>3役に分けてあることの見返りがここで出ます。この章の完了時点で <code>Tests run: 43</code> が緑です（Tomcat が止まっていると <code>ItemHttpIT</code> の6件が消えて 37 になります）。</p><hr><h2 id="第12章" tabindex="-1">第12章 <a class="header-anchor" href="#第12章" aria-label="Permalink to &quot;第12章&quot;">​</a></h2><blockquote><p>対象: <a href="./../chapters/12-spring-boot">第12章 Spring Boot は何を肩代わりしていたか</a></p></blockquote><p><strong>1. <code>@SpringBootApplication</code> は3つのアノテーションをまとめたものである。3つ挙げ、そのうち本編にも書いていたものと本編に無かったものを分けよ。</strong></p><p>3つは <strong><code>@Configuration</code></strong>（設定クラスである）、<strong><code>@ComponentScan</code></strong>（このパッケージ以下を走査する）、<strong><code>@EnableAutoConfiguration</code></strong>（クラスパスにある JAR を見て設定を推測する）です。このうち <strong><code>@Configuration</code> と <code>@ComponentScan</code> は本編にも書いていました</strong>——7章の <code>WebConfig</code> がまさにそれです。<strong>本編に無かったのは <code>@EnableAutoConfiguration</code> の1つだけ</strong>で、Boot の「魔法」に見える部分はほぼ全部これに由来します。逆に言えば、あなたは3つのうち2つをすでに手で書いていたわけです。</p><p><strong>2. 本編では <code>main</code> を誰が持っていたか。Boot では誰が持っているか。それでも「制御の反転」が解消されていないのはなぜか。</strong></p><p>本編では <strong>Tomcat（<code>org.apache.catalina.startup.Bootstrap</code>）</strong> が <code>main</code> を持ち、その中の <code>webapps/</code> に置かれた WAR としてあなたのコードが呼ばれていました（→ <a href="./../chapters/03-container">3章</a>）。Boot では <strong><code>InventoryApplication</code>——あなたが書いたクラス</strong>が <code>main</code> を持ち、<strong>Tomcat のほうがアプリの中に埋め込まれて起動します</strong>（外側と内側が入れ替わった）。それでも制御の反転が解消されていないのは、その <code>main</code> がやっているのが <strong><code>SpringApplication.run(...)</code> を呼ぶことだけ</strong>だからです。そこから先は Spring があなたの <code>@Controller</code> や <code>@Service</code> を呼び出します。<strong>入り口だけが自分のもので、中身の制御は相変わらずコンテナが持っている。反転は解消されておらず、起動の仕方が変わっただけ</strong>です。</p><p><strong>3. Boot 版でコンテキストパスが空になったのはなぜか。その結果、<code>ItemController</code> に何を足す必要があったか。</strong></p><p><strong>Boot は WAR を <code>webapps</code> に置かないから</strong>です。3章で確認したとおり、コンテキストパスは「WAR のファイル名（<code>finalName</code>）が <code>webapps</code> に置かれること」で決まっていました。その仕組みが働かないので、起動ログは <code>Tomcat started on port 8080 (http) with context path &#39;&#39;</code> になり、URL も <code>http://localhost:18081/inventory/app/list</code>（本編）から <code>http://localhost:18082/app/list</code>（Boot）に変わります。足す必要があったのは、クラスへの <strong><code>@RequestMapping(&quot;/app&quot;)</code></strong> です。本編では <code>web.xml</code> の <code>&lt;url-pattern&gt;/app/*&lt;/url-pattern&gt;</code> が <code>/app</code> を担当していたので <code>@GetMapping</code> には「その先」だけ（<code>&quot;/list&quot;</code>）を書いていましたが、<code>web.xml</code> が消えたぶんを自分で書き直したわけです。<strong>「設定が消える」ことは「設定が別の場所に移る」ことでもあります。</strong></p><p><strong>4. <code>setDriverClassName(&quot;org.postgresql.Driver&quot;)</code> を書かなくてよくなったのはなぜか。</strong></p><p><strong><code>spring.datasource.url</code> の <code>jdbc:postgresql:</code> という接頭辞から、Boot がドライバのクラス名を推測している</strong>からです。これが「自動設定（auto-configuration）」の実例です。8章では、この行を書き忘れて <code>Failed to get driver instance for jdbcUrl=jdbc:postgresql://db:5432/inventory</code> で起動に失敗しました。便利ですが、<strong>8章を飛ばして Boot から始めていたら、「ドライバのクラスをロードする」という工程がそこにあること自体を知らないまま</strong>、いつか本番で似たエラーに数時間溶かすことになります。あなたはそれを自分の手で書いて、失敗して、直したので知っています。</p><p><strong>5. Boot にしても消えなかったものを3つ挙げよ。</strong></p><p>(1) <strong>業務そのもの</strong>——<code>DailyClosingConfig</code> の Job / Step / Reader / Processor / Writer の定義は本編とまったく同じですし、<code>ItemService.register</code> の「コードが重複していたら登録しない」も同じです。(2) <strong>設計の判断</strong>——<code>ItemRepository</code> をインターフェースにするか、<code>ItemForm</code> と <code>Item</code> を分けるか、メール送信が失敗したとき在庫の更新を巻き戻すか。<strong>これらは全部あなたが決めたことで、Boot は何も言いません。</strong> (3) <strong>落とし穴（ライフサイクルの理解）</strong>——11章の <code>@StepScope</code> を忘れると Boot でも同じように「起動した瞬間の在庫を握ったまま固まる」ので、フレームワークの問題ではなく Bean のライフサイクルの問題です。加えて (4) <strong>starter が無い依存</strong>——JAXB は Boot でも自分で <code>&lt;dependency&gt;</code> を書きました。</p><p>実際、本編から <code>app-boot/</code> にコピーした <strong>12個のファイルのうち、変更が必要だったのは2個だけ</strong>（<code>ItemController</code> に <code>@RequestMapping(&quot;/app&quot;)</code> を足し、<code>DailyClosingConfig</code> から <code>@EnableBatchProcessing</code> を消した）で、<strong>残り10個は1文字も変えずに動きました</strong>。6章で「テストが書けない」という困りごとから自分で導いた設計は、フレームワークを変えても生き残ります。</p><p><strong>6. <code>spring.jpa.open-in-view</code> は何を有効にする設定か。それは8章のどの概念に関係するか。</strong></p><p><strong>永続化コンテキストを、画面のレンダリングが終わるまで開きっぱなしにする</strong>設定です（→ <a href="./../chapters/08-jpa">8章</a>）。Boot では<strong>既定で有効</strong>で、起動ログに次の警告が出ます。</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>WARN ... spring.jpa.open-in-view is enabled by default. Therefore, database queries may be performed during view rendering.</span></span></code></pre></div><p>何が起きるかというと、JSP や Thymeleaf のテンプレートの中で <code>${item.something}</code> と書いたときに<strong>そこで DB アクセスが発生しうる</strong>ということです。画面を描くたびに気づかないうちにクエリが増える（N+1 問題の温床）。<strong>本編にはこの設定がありませんでした。自分で <code>JpaConfig</code> を書いたので、書いていないものは存在しなかった</strong>からです。<strong>Boot では、書いていないものが存在します。これが最大の代償です。</strong> <code>spring.jpa.open-in-view=false</code> で無効にできますが、意味を理解してからにしてください。</p><p><strong>7. Boot の起動が本編より遅いのはなぜか。</strong></p><p><strong><code>@EnableAutoConfiguration</code> がクラスパスを走査して「どの JAR があるか」から設定を推測している</strong>からです。その推測のために、起動のたびに数百個の判定が走ります。本編は「書いてあることをそのまま実行する」だけなので、その分は要りません。実測は次のとおりです。</p><table tabindex="0"><thead><tr><th></th><th>起動時間</th></tr></thead><tbody><tr><td>本編: Tomcat の起動</td><td><strong>0.676秒</strong>（序章の実測）</td></tr><tr><td>本編: DispatcherServlet の初期化</td><td><strong>8.327秒</strong>（7章の実測。最初のアクセス時）</td></tr><tr><td>Boot</td><td><strong>11.224秒</strong></td></tr></tbody></table><p><strong>便利さには計算コストが伴います。</strong> 開発中は気にならない差ですが、起動と停止を繰り返す環境（サーバーレスなど）では効いてきます。成果物のサイズも、本編の <code>inventory.war</code> が約 39.7 MB なのに対し、Boot の <code>inventory-boot-1.0.0.jar</code> は約 58.0 MB と大きくなります。Tomcat を内包しているぶんです。</p><p><strong>8. JSP を2章で学んだことは、Thymeleaf を読むときにどう役立ったか。</strong></p><p><strong>書き方が違うだけで、やっていることは同じだと分かる</strong>ようになりました。読み替えるだけで済みます。</p><table tabindex="0"><thead><tr><th>2章・9章で書いた JSP</th><th>Thymeleaf</th></tr></thead><tbody><tr><td><code>&lt;c:forEach var=&quot;item&quot; items=&quot;${items}&quot;&gt;</code></td><td><code>th:each=&quot;item : ${items}&quot;</code></td></tr><tr><td><code>&lt;c:out value=&quot;${item.name}&quot;/&gt;</code></td><td><code>th:text=&quot;${item.name}&quot;</code></td></tr><tr><td><code>&lt;c:if test=&quot;...&quot;&gt;</code></td><td><code>th:if=&quot;...&quot;</code></td></tr><tr><td><code>&lt;c:choose&gt;&lt;c:when&gt;</code> / <code>&lt;c:otherwise&gt;</code></td><td><code>th:if</code> / <code>th:unless</code></td></tr><tr><td><code>&lt;form:input path=&quot;code&quot;/&gt;</code></td><td><code>&lt;input th:field=&quot;*{code}&quot;&gt;</code></td></tr><tr><td><code>&lt;form:errors path=&quot;code&quot;/&gt;</code></td><td><code>th:errors=&quot;*{code}&quot;</code></td></tr><tr><td><code>${pageContext.request.contextPath}/app/list</code></td><td><code>@{/app/list}</code></td></tr></tbody></table><p>とくに <strong><code>th:text</code> は既定でエスケープする</strong>という設計の意味が分かります。2章で <code>${keyword}</code> を裸で書いて XSS の穴を開け、<code>&lt;c:out&gt;</code> で塞いだ体験があるからです。エスケープしないで出したいときだけ <code>th:utext</code> を使う——**「安全なほうが既定」**という設計だと読めます。そして2章で「JSP の正体は Servlet で <code>out.write(...)</code> の羅列に変換されている」と <code>work/</code> を覗いて確かめたとおり、Thymeleaf も <strong>HTML の中に値を差し込むという構造は変わっていません</strong>。</p><hr>', 573)
  ])]);
}
const checks = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  checks as default
};
